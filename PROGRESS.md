# Yarım İşler — İlerleme Defteri

> 2026-08-22 durum tespitiyle çıkarılan ve kullanıcının onayladığı yarım işlerin kaydı.
> Tam liste ve gerekçeler: `docs/backlog.md`. Sıra kullanıcı onayıyla:
> #1 → #2 → #13 → #14 → #3 → #4 → #6 → #12 → #5.
> Kapsam dışı (kullanıcı kararı): #7 lint (hayır), #8 push (sonra), #9/#10/#11 (seçilmedi), #15 R2 (en sona).

| # | İş | Durum | Not |
|---|---|---|---|
| 1 | Sessiz videoda "Sesi ayır" tuzağı (`hasAudio` editöre bağlandı) | ✅ YAPILDI | Önce ölçüldü (gerçek fare → 422 `asset-clip-type`), sonra kapatıldı; menü gri + Türkçe gerekçe; birim+e2e+negatif kontrol. Editör 1306 / şema 212 / tsc temiz. |
| 2 | Inspector "ripple'sız en yavaş hız" sınırı yarım kare eksik | ✅ YAPILDI | Önce ölçüldü (backlog'un fazı frame 0'da: panel 0.5x önerdi, op "sonraki klibe giriyor" dedi). Sınır artık op'un KENDİ planlayıcısıyla türetiliyor (`minSpeedRateWithoutRipple`: `planClipSpeed` kabul kâhini, ikinci aritmetik kopyası yok); öneri her zaman uygulanabilir, bir ızgara adımı yavaşı RET (30/29.97/24 fps 144'lük tarama). Gerçek-klavye e2e: 0.497 RET + 0.498 KABUL. Negatif kontrol md5 birebir. Editör 1313 / tsc + e2e tsc temiz. |
| 13 | 503 font-kökü varsayımına sağlık kontrolü | ✅ YAPILDI | "API ile worker AYNI kökü görür" varsayımı artık ölçülebilir: iki uç aynı türetimi (`FontRootHealth` — fontId/dosya sayıları + manifest+lock pin setinin sha256 parmak izi) raporlar; API `GET /health` `fonts` bölümü (font eksiğinde de 200 — rapor dürüst, kapı değil), worker açılış satırı. RPC yok; karşılaştırma işletmecide (deploy/README §5.2 adım 4). Canlı ölçüldü: sağlam kökte iki taraf birebir aynı parmak izi (`f862…861d`), env'siz ikinci API süreci `found:false` + bakılan yol + sebep döndü. 5 yeni test (köklü/köksüz uç + lock tutarlılığı + sıra bağımsızlığı); negatif kontrol: iki mekanizma ayrı ayrı kırmızı, md5 birebir geri. Backend 1420/1420, skip 0. |
| 14 | Kod yorumlarında tur numarası ayrışması | ✅ YAPILDI | Kaynak koddaki (apps/backend/packages) TÜM tarihsel tur/dalga/"M6 denetimi" yorum atıfları kalıcı gerekçeye çevrildi (ölçülmüş sayılar korundu); milestone etiketleri ve bulgu numaraları (#1, N1 — arşiv kimliği) yerinde. Test ADLARI ve dizge literalleri bilerek dokunulmadı (davranış sıfır). Mekanik kanıt: diff'te (75 kod dosyası, 181+/171−) yorum-olmayan değişen satır sayısı 0 (scratchpad taraması). Build -warnaserror 0/0; backend 1420/1420 skip 0 (MinIO'lu); editör 1313 + şema 212; tsc -b temiz. Backlog kaydı KAPANDI. |
| 3 | 2160p bellek kabul kapısı | ✅ YAPILDI | Önce ölçüldü (10 gerçek render, `PeakWorkingSet64` — perf'in 500 ms örneklemli 4 908'i alt sınırmış): tepeyi süren SÜRE değil, çıktı pikselleri (düz kesim 461→910→2 921 MB) + eşzamanlı görsel giriş × TUVAL pikselleri (bileşim profilden bağımsız +2,1-2,5 GB; 2160p bileşim 5 563 MB). Kapı `EnsureMemoryAsync` (disk deseninin eşi): ölçüme sabitli üst-bant tahmin (`EstimateRequiredMemoryBytes` + eşzamanlılık süpürmesi `MemoryEstimateInputs` — ardışık 500 klip 500 çözücü SAYILMAZ) vs kullanılabilir bellek; `memory-wait` erteleme, son denemede tipli `insufficient-memory`. Windows okuyucusu İKİ canlı ölçümle commit boşluğu (`ullAvailPageFile`) seçildi: `ullAvailPhys` sürümü canlıda 2160p'yi YANLIŞ reddetti (fiziksel-boş 4,9 GiB'ken aynı render 2× başarılıydı; commit boşluğu 38,7 GiB) — yanlış-ret kanıtla kapatıldı. Linux min(MemAvailable, cgroup v2/v1; compose 6g limiti sonucu §4.6'da beyan). Kanıt: canlı korpus sabitli + `ProcessStarted` kancalı gerçek-render testleri + kapılı ikilide demo/plain/comp × 720p/1080p/2160p canlı BAŞARILI (memory-wait görülmedi); negatif kontrol 2 kez, md5 birebir. Backend 1432/1432 skip 0; editör 1313 + şema 212; tsc temiz. |
| 4 | media-urls manifest optimizasyonu | ✅ YAPILDI | Önce ölçüldü (perf yöntemi; 55'lik proje ham API'yle): mevcut paralel hal 1/15/55 asset p50 4,24/12,82/34,72 ms — doğrusal; pay ayrıştırması baskın payın manifest GET'i (1,07 ms/obje) değil presign CPU'su (245 µs/çağrı × ~7/asset) olduğunu gösterdi. Çözüm: manifest işlenirken `Assets.FilmstripManifest` jsonb kolonuna da yazılıyor (migration; NULL = eski asset → storage-GET YEDEK yolu, backfill bilinçli yok) + media-urls döngüsü `Task.Run` ile gerçek CPU paralelliğinde (eşzamanlı presign güvenliği ölçümle: 16 iş parçacığı × 32k çağrı sıfır hata, imzalar MinIO'dan 200). Sonra: yeni asset'lerde **2,33/3,93/9,30 ms** (55'te 3,7×; marjinal 0,56 → 0,13 ms/asset; rapor dönemine göre ~13×), eski asset'ler yedek yolda 17,06 ms; yanıt şekli önce↔sonra birebir (istemci değişmedi). İki yol da testli + MinIO'lu pipeline kanıtı (jsonb ↔ storage manifest eşitliği); negatif kontrol: DB yolu geri alınınca 5 test kırmızı, md5 birebir geri. Backend 1438/1438 skip 0; editör 1313 + şema 212; tsc temiz; canlıda demo media-urls 200 + örnek presigned GET 206. |
| 6 | IDOR regresyon test paketi | ✅ YAPILDI | `CrossUserAccessTests`: kurban A + saldırgan B; 24 kimlik-doğrulamalı ucun TAMAMINA A'nın id'siyle B çağrısı (proje CRUD/timeline/revisions/restore, asset yaşam döngüsü/media-urls/usage/sil, export başlat/listele/izle/iptal, quota) — yalın 404 + kurban durumu değişmedi + tuzaklı depo sahtesi ("reddedilen istek presign'a hiç ulaşmadı" kanıtı). TAMLIK MUHAFIZI: rota tablosu MEKANİK (ürünün `Map*Endpoints` metotları refleksiyonla keşfedilip boş WebApplication'da koşturulur); her uç ya cross-user testinde ([Fact] refleksiyonla doğrulanır) ya gerekçeli muafta (id parametreli uç muaf OLAMAZ) ya bilinçli-anonim defterinde; Program.cs taraması satır-içi rota kaçağını kapatır. 3 negatif kontrol, md5 birebir geri: filtre gevşet → 2 test kırmızı (presign sızıntı imzası); `/steal` ucu → muhafız kırmızı; quota auth'suz → muhafız kırmızı. Ürün davranışı sıfır değişiklik (yalnız handler görünürlüğü internal + AppDbContext test-provider dalına DateTimeOffset converter). Build -warnaserror 0/0; backend 1464/1464 skip 0 (MinIO'lu). Backlog M2 kaydı KAPANDI. |
| 12 | GenerateDocumentationFile / XML doc borcu | ✅ YAPILDI | Önce YENİDEN ölçüldü (6 src projesi, ayrı BaseOutputPath; bayat "~21+~230" tutmadı): 443 benzersiz = 404 CS1591 + 32 CS1573 + 7 CS0419 (CS1572/1574=0). 39 YAPISAL uyarının tamamı kapatıldı — eksik `<param>`lar koddan türetilen gerçek içerikle yazıldı (`<remarks>`e çevirme reddedildi: kurulan denetimi silerdi), 7 belirsiz `Compile` cref'i imzayla ayrıştırıldı. Mekanizma `backend/src/Directory.Build.props`: `GenerateDocumentationFile=true` + `NoWarn CS1591` (gerekçesi props yorumunda; 404 şablon özet yazılmadı — bilinçli susturma) + yapısal küme `WarningsAsErrors` (CS0419/1570/1572/1573/1574/1587) → ihlal CI'ın sade `dotnet build` adımında da hata; ci.yml değişmedi (props'u sln build otomatik alır, Docker `COPY . .` ile alır). tests/tools BİLEREK kapsam dışı (ölçüldü: ~902 CS1591 + 29 yapısal test-iskelesi borcu açardı; tüketicisiz yüzey). Bilinen yan etki beyan edildi: dev-only /openapi/v1.json XML özetlerini kazanır (üretim map etmez, test okumaz, generator'da kapatma anahtarı yok). Negatif kontrol: kasıtlı CS1573 → sade build DE -warnaserror DE kırmızı; md5 birebir geri. Davranış sıfır (diff: yalnız XML yorumları + props + doküman). Build -warnaserror 0/0; backend 1464/1464 skip 0 (MinIO'lu). Backlog kaydı KAPANDI. |
| 5 | Export filtre grafiği performansı (EN SON — golden'lar elle) | ✅ YAPILDI | Önce maliyet profili çıkarıldı (bisect: canlı worker'ın gerçek grafiği + birebir girdiler; %46 = `blend=all_expr` per-piksel yorumlayıcısı, %19 örtülü metin+şekil zincirleri, %10 colorAdjust, %6 kodlayıcı) ve rapor bağımsız DOĞRULANDI (kendi koşumum: taban 67,6-68,7 s vs yerli blend 37,4 s). Uygulanan tek değişiklik: §4.2 LUT karışımı `blend=all_expr` → yerli `blend=all_mode=normal:all_opacity=1-intensity` (`ClipEffects.LutBlendFilter`; formül değişmedi, yazılışı değişti — rendering-semantics §4.2 güncellendi). EŞDEĞERLİK İÇERİĞE BAĞLI (2026-08-25 doğrulama turu, tam (A,B) taraması kendi koşumum): dyadik intensity'de (0.25/0.5/0.75) bayt-aynı; dyadik olmayanda tamsayı-denk çiftlerde tam ±1 LSB — i=0.8'de 65.536 çiftin 1201'i (%1,8), i=0.6'da 208'i (%0,3; ör. A=1,B=6 → expr 3, yerli 4); zarf `ExportM5GoldenTests.LutBlend_NativeVsExpr_BoundedByOneLsb…` golden'ıyla sınırlandı (uyuşmazlık çiftlerine bilerek düşer; ±1 LSB, §9.3 parite eşiklerinin ve RGB↔YUV420 ±1 sınıfının içinde). lut.spec 2026-08-25'te yeniden koşuldu (gerçek fare, canlı run28 yığını): 2/2 yeşil; parite SSIM(gri)=0,99424, kanal |fark| ort=1,603, p99=4 — eşikler ≥0,98 / ≤2,0 rahat sağlanıyor; NOT: testin export bacağı intensity=1'dedir (blend hiç üretilmez, düz lut3d), yani parite blend yazılışından bağımsız; blend'li yolu önizleme-içi ±3 yoğunluk ölçümü (slider ~0.5) + LSB golden'ı sınırlar. Bench framemd5/canlı sha256-aynı ölçümleri O İÇERİKLERE ÖZGÜ (uyuşmazlık çifti üretmiyorlar), genel "bayt-aynı" iddiası DÜZELTİLDİ. Canlı önce/sonra (perf yöntemi, 3'er koşum, run27→run28): **720p 0,89x→1,69x; 1080p 0,87x→1,60x; 2160p 0,76x→1,27x**; düz kesim 11,5x etkilenmedi; aynı belgenin iki ikilideki çıktı MP4'leri sha256-aynı, worker grafik diff'i tek satır. ÖLÇÜMLE İPTAL edilen yönler: no-op scale/pad/fps eliminasyonu (kazanç 0; scale'siz BT.601 kayması + §2.5 ihlali), enable daraltma (≤2-3 s), threads ayarı (varsayılan optimum). Örtülen-katman budaması (−13,2 s ölçülü) §2.5 kaynak-bağımsızlık sözleşmesiyle çeliştiği için BİLİNÇLİ yapılmadı → backlog'da baş mimar kararına bağlandı; colorAdjust füzyonu ölçülmedi (spekülatif), tuval-atlama bayt değiştirir — ikisi de backlog'da. Negatif kontrol (2026-08-25'te TAM pakette YENİDEN ölçüldü; önceki "3 bekçi" beyanı `Lut3d…` golden'ını saymıyordu): yalnız `LutBlendFilter` gövdesi geri alınınca **6 test kırmızı** (tüm-fixture `all_expr`/`geq` taraması + TR-kültür literal testi + lut-effects snapshot + `Lut3d_AppliesTheCubeFile…` + LSB sınır golden'ı + GateInventory kaynak-token envanteri), geri konunca dosya MD5 birebir + paket yeşil. Build -warnaserror 0/0; backend **1496/1496 skip 0** (MinIO+ffmpeg; snapshot/golden dahil). |

## B borçları

> Yarım-iş turu kapandıktan sonra açılan, `docs/poc-bilinen-sinirlar.md`'nin "ölçülmedi"
> kayıtlarını kapatma dilimleri.

| # | İş | Durum | Not |
|---|---|---|---|
| B1 | Ses paritesi (önizleme ↔ export) İLK KEZ ölçüldü (§2.6 borcu) | ✅ YAPILDI | İki bağımsız gerçekleme aynı belge üzerinde karşılaştırıldı: önizleme tarafı gerçek Chromium'da OfflineAudioContext + uygulamanın KENDİ modülleri (`buildGainCurve`/`shouldMicroFade*`/`clipAudioOf` Vite modül grafiğinden; topoloji AudioGraph'la aynı; kaynak = oynatıcının çözdüğü PROXY; `createMediaElementSource` offline'da var olmadığı için AudioGraph'ın kendisi koşturulamaz — gerekçe spec başlığında), export tarafı gerçek POST /exports → worker → ffmpeg → indirilen MP4. 9 vaka × 100 ms RMS + 10 ms zarf çapraz korelasyonu: hiza rate=1'de 3,2 ms, atempo'da 9,4 ms; düz/miks/fade/kırpma vakalarında \|Δ\|max ≤ 0,70 dB; limiter vakası §8.3 asimetrisinin ölçümü (önizleme tepe 1,163, export 0,950; +0,28 dB); hız 2x \|Δ\|max 1,24 dB. DUYULUR SINIF FARK YOK → açık bulgu yok; sistematik +0,175 dB adlandırıldı (alimiter varsayılan auto-level = 1/limit; ayrı ffmpeg ölçümüyle doğrulandı) ve rendering-semantics §8.3'e ölçülmüş yan etki olarak yazıldı. §2.6 KAPANDI: normatif sınır tablosu dokümanda, muhafızı `e2e/audio-parity.spec.ts` (eşikler tabloyla birebir). Muhafız e2e seçildi (backend belge-türevi zarf değil): backend testi export'u aynı matematiğin yeniden yazımıyla karşılaştırır, önizleme gerilemesini göremezdi. Negatif kontrol: `fadeEnvelopeAt` eğimi kasıtlı yarıya indirildi → fade-in vakası 6,42 dB ile kırmızı → md5 birebir geri (`ea1bc3…0d21`). Komşu borç da kapandı: demo medyasına `demo-04-muzik.m4a` (make-demo-media ps1+sh), senaryoya Adım 4M, omurga (1→4, 4M, 17) gerçek fare/klavyeyle koşuldu (çıktıda müziğin 220 Hz bandı −18,6 dBFS; kayıt demo-senaryosu §7). Editör 1313 + şema 212 + tsc + e2e tsc temiz; ses e2e üçlüsü (parity/audio-export/detach-audio-silent) yeşil. Backend'e DOKUNULMADI. |
| B5 | Çıktı-saati bekçisi işleme reçetelerine | ✅ YAPILDI | proxy=kaynak süresi, filmstrip=SPRITE saati (ExpectedOutputClockUs — ölçüldü: naif kaynak-süresi tavanı yanlış öldürürdü), waveform=PCM bayt tavanı; poster ölçülerek tavansız; aşım tipli `transcode-overrun`; korpusa YALAN-SÜRE beyanlı m4a/mp4 sınıfı (yalnız tavan yakalar); 7 eski pis dosya yanlış öldürülmedi; negatif kontrol 3 kırmızı → md5 birebir geri. Backend 1511/1511 skip 0. |
| B2 | 1-2 GB ölçümü n=1'di: tekrarlanabilirlik + >2 GB / 4 GiB tavanı | ✅ YAPILDI | (1) KALICI PERF TESTİ: `MediaPipelinePerfTests` (MinIO+ffmpeg kapılı, CI'da da koşar) her koşumda ~320 MB'lik 1080p30 20 Mbps grenli kaynağı üretip ürünün yolundan geçiriyor — 64 MiB'lık 5 parça, 4-eşzamanlı presigned PUT (istemci motorunun sunucu eşi), `ProcessAssetJob`, 30 sn'lik `ExportJob` (soğuk cache); iddialar ORANSAL ve eşikler kendi 3 izole koşumumdan türetildi (işleme/kaynak 0,097-0,107 → tavan 0,75; export/çıktı 0,23-0,25 → 2,0; türev/orijinal %7,46-7,54 → %25; yükleme ≤ kaynak süresi — felaket kapısı; gerekçeler sabitlerin xmldoc'unda); iş-sonrası worker-dizini-temiz iddiası da içinde. Tam-suite PARALEL yükte oranın komşu testlerin fonksiyonu olduğu ölçülünce (B6 koşumundaki kırmızı + kendi 0,183 gözlemim) test `DisableParallelization` koleksiyonuna alındı: sessiz şerit, bedeli ~1 dk suite süresi. (2) 4 GiB TAVANI CANLI (HEAD yayını `api-run-b2`, tazelik yüklü modülde B5 iğnesiyle): tam 4 GiB init → 201 + partCount 64 (abort'landı); 4 GiB+1 → İSTEK ANINDA 400 (32 ms, doğru sayılı mesaj); 4 295 098 368 B GERÇEK sparse dosya gerçek dosya seçicisinden → init 400 (4,8 ms), tek bayt yüklenmedi, kart "Başarısız — Yükleme başlatılamadı: sizeBytes must be…" (mesajın İngilizceliği backlog'a düşük kayıt). (3) n=2 UÇTAN UCA (2,53 GB / 1000 sn gren, gerçek fare + gerçek seçici + ürün motoru): 38 parça yükleme 6,73 sn (~375 MB/sn lokal), kuyruk 0,6 sn, işleme 109,8 sn = 9,1× (proxy 64,0 + filmstrip 38,4 — 4 sprite; aşamalar 0,4 sn yoklayıcıdan), "Hazır"a 117,6 sn, timeline'a alma 0,13 sn (klip tam 1000 sn), cetvel+ok tuşlarıyla KARE-KESİN 60 sn bölme, 60 sn kesim export'u iş 18,1 sn / duvar 23,3 sn, çıktı 214 535 418 B ≈ 28,6 Mbps (n=1 ile tutarlı: 9,3× / %7,7 / 215 311 057 B); türev defteri %7,48 MinIO ile uyumlu; kota +2 716 741 006 doğru saydı. (4) NEGATİF KONTROL: işleme tavanı 0,001'e çekildi → kırmızı ("işleme oranı 0.183 > tavan 0.001") → md5 birebir geri (9DF6…81DF). TEMİZLİK (disk kanıtıyla): asset+2 proje ürün yolundan 204; kota 5 343 045 574 → 2 626 304 568 B (koşum öncesi değer); MinIO media 30 161 137 528 → 27 444 219 593 B, exports 18 217 078 743 → 18 002 524 127 B, koşum prefix'lerinde 0 obje; DB'de koşum satırları 0; LRU'daki 2,53 GB girdi + scratchpad'teki 2,53 GB kaynak + 4,3 GB sparse silindi; perf-e2e bucket'ları boş; worker dizini boş; kaçak ffmpeg yok. Dokümanlar: poc §0.1.1 (n=2 + tavan) + §0.1.2 (iddia→muhafız tablosu: neler kalıcı, neler hâlâ elle) + §5.1/§6; backlog 12. tur üç kaydı (n=1 KAPANDI, >2 GB/4 GiB KAPANDI, korunmuyor-borcu perf yarısı KAPANDI — "sonradan düzenleme" e2e yarısı bilinçli AÇIK). Build -warnaserror 0/0; backend TAM suite **1517/1517 skip 0** (MinIO+ffmpeg; yeni perf testi sessiz şeritte dahil; ağaçta B6'nın worker değişiklikleriyle birlikte). |
| B-kenar/lut | lut.spec'in export bacağı intensity=1'deydi (blend hiç üretilmiyordu) | ✅ YAPILDI | Dyadik OLMAYAN yoğunlukta ikinci gerçek-fare export bacağı eklendi: slider gerçek fareyle ~0.8'e (ilk koşum 0.82'ye indi), gerçek klavye oklarıyla [0.77-0.81] bandına — koşumda doc'a 0.81 yazıldı (I·64 tam-sayı-değil dyadik muhafızı testte). Böylece worker'ın YERLİ blend yolu (`LutBlendFilter`, `all_opacity=1-I` — intensity=1'de hiç kurulmaz) ilk kez e2e'de koşuyor. Kanıt: indirilen MP4'ün ilk karesinde iki AYNALI 16×16 yamada ((1872,54) ΔB≈+36 / (96,54) ΔB≈−34; referanslar LUT'suz önizleme karesinden `sampleCubeLut` §4.2 ile) — her kanal kısmî referansa oturdu (\|sapma\| 0,21-2,49 kod ≤ 4,0), ham↔tam zarfı ±1 LSB payıyla içeride, B kanalı ne ham ne tam (tam'a 7,18/4,45, ham'a 29,22/29,86 ≥ 2,5). Tolerans 4,0 İLK KOŞUM ÖLÇÜMÜYLE kalibre (sapmalar 0,99-2,95 kod, tümü aynı yönde — x264 düz-bölge DC nicemleme kayması blok-korelasyonlu, yama ortalaması süzemez); kısmîlik iddiası R'den çekilip yalnız aynalı güçlü B'ye kondu (R'de tam'a mesafe ≈4,3 kod, yanlılıkla ayırt edilemez) + ayrıştırma-gücü muhafızı (tam↔kısmî aralık ≥ pay+yanlılık bandı, fixture değişirse bacak sessizce boşalamaz). Ortak export akışı `exportAndDownload` yardımcısına alındı (ilk bacak davranışı değişmedi: parite SSIM 0,99424 / ort 1,603 / p99=4 — önceki sınıfın aynısı). NEGATİF KONTROL: `writeLutIntensity` etkisizleştirildi → sürükleme + 8 gerçek ok doc'a yazamadı → "yoğunluk yazımı hedef banda inmedi (Received: 1)" KIRMIZI → md5 birebir geri (BE5649…ED54; `git checkout` CRLF smudge'ı LF'e normalize edilerek bayt-birebir). Canlı ortam HEAD'in TEMİZ KLONUNDAN yayınlandı (`api-run-b4lut`/`worker-run-b4lut`; ağaçtaki başkasına ait worker WIP'i bilinçli dışarıda) — tazelik yüklü modül yolu + blend UTF-16 iğnesi VAR + WIP iğnesi YOK ile kanıtlı. lut.spec TAM 3/3 yeşil (51,8 sn); e2e tsc temiz; kaçak ffmpeg yok. |
| B4 | ci.yml'de doğrulanamayan sayı ("temiz klonda 139 test bunsuz kırılır") | ✅ YAPILDI | ÖNCE ÖLÇÜLDÜ (2026-08-25; temiz klon = HEAD 395c246'nın scratchpad'e yerel `git clone`'u): (a) `pnpm install --frozen-lockfile` prepare'i KOŞUYOR ve dist'i üretiyor (5,7 sn) — adımın yokluğu temiz klonda tek başına hiçbir şeyi kırmıyor, eski yorumun "temiz klonda bunsuz kırılır" öncülü bugün yanlış; kırılma ancak dist YOKKEN (ignore-scripts/bozuk prepare/temizlenmiş dist). (b) dist yokken `pnpm -r test`: şema 3 dosya/212 test YEŞİL (testlerini `../src`'den import eder), editörde 39/77 dosya daha import/toplama aşamasında KIRMIZI, kalan 38 dosyanın 400 testi geçiyor — bugünkü 1313 testin 913'ü hiç koşamıyor ve vitest toplanamayan dosyanın testlerini SAYMIYOR: "N test kırılır" aracın çıktısında tanımlı bir büyüklük bile değil. (c) dist geri konunca 3+77 dosya / 212+1313 test yeşil — ölçüm çiftinin kendisi negatif kontrol. KARAR: SAYISIZLAŞTIRMA (ölç-ve-sayı-yaz değil): birimi belirsiz ve paketle büyüyen sayı (139 → bugün 39 dosya/913 koşamayan) CI yorumunda doğrulanamadan bayatlıyor; yorum artık mekanizmayı ve sayının bilinçli yokluğunu anlatıyor, ölçüm kaydı commit mesajında. Davranış değişikliği sıfır (yalnız yorum). |
| B7 | Tahmin sabitleri bu makinede ölçülmüştü — worker config'inden ayarlanabilir | ✅ YAPILDI | `EstimateRequiredMemoryBytes`'ın beş `Memory*` sabiti + `EffectiveOutputBitsPerSecond`'un dört profil tabanı "ExportEstimates" section'ından (env: `ExportEstimates__*`) ezilebiliyor; FORMÜLLER DEĞİŞMEDİ, null = ölçülen varsayılan (tek kaynak ExportJob const'ları + `ExportProfiles.EstimatedBitsPerSecond` — kopya yok, ölçüm güncellenince ayrışamaz). Etkin değerler worker açılışında loglanıyor (FontRootHealth görünürlük deseninin log yarısı); pozitif olmayan override açılışta düşürüyor (prod-guard deseni). Testler: ölçülen varsayılanlar LİTERAL çivili (268 435 456 / 400 / 4 194 304 / 48 / 400 B + 10/5/40/10 Mb/s); override yalnız kendi terimini oynatıyor (elle hesaplı literaller); env-şekilli binding + mekanik tamlık (yeni knob binding testine girmeden eklenemez); kapı DI'dan gelen sabitlerle KARARINI iki yönlü değiştiriyor (sabitler 1'e ezilince 512 MB'lik probe'da geçti; kodlayıcı 1 MB/px'e şişirilince 64 GiB'ta memory-wait). Negatif kontrol: options okuması geri alınınca 4 test kırmızı → md5 birebir geri (`50b430fe…272c`) + touch+rebuild → 14/14 yeşil. Build -warnaserror 0/0. |
| B8 | media-urls'te 2069 eski asset yedek (storage-GET) yolundaydı — tembel backfill | ✅ YAPILDI | Değerlendirildi ve UCUZ bulundu (≈40 satır ürün + 3 test) → uygulandı: yedek yol bir asset'in manifest.json'ını storage'dan okuduğunda aynı içerik `Assets.FilmstripManifest` jsonb kolonuna da yazılıyor (paralel presign bölgesinin DIŞINDA — "döngüde DbContext yok" kuralı korunuyor; toplu migration bilinçli YOK, `Asset.cs` sözleşmesi güncellendi). Yazım best-effort: bozuk JSON/DB hatası yanıtı DÜŞÜRMÜYOR, yutulup gerekçeli loglanıyor (iptal yutulmaz); yarış korumalı (yalnız kolon hâlâ NULL ise yazılır — işleme hattının kopyası ezilmez; FakeStorage `OnOpenRead` kancasıyla kurulan gerçek yarış testi). ASIL KANIT: aynı asset'in İKİNCİ media-urls çağrısı artık DB'den — OpenReadCount 1'de kalıyor, yanıt şekli birebir; bozuk manifest'te kolon NULL kalıp yedek yol aynen sürüyor (OpenReadCount 2'ye çıkıyor). Negatif kontrol: backfill çağrısı kaldırılınca self-heal testi kırmızı → md5 birebir geri (`2b45cbf0…3af7`) + touch+rebuild → 38/38 yeşil. Canlıdaki 2069 satır, B8'li ikili bir SONRAKİ yayında İSTENDİKÇE kendiliğinden iyileşir (canlı servis yönetimi bu dilimin bilinçli kapsam dışı — görev backend-only). Build -warnaserror 0/0. |
| B-kenar/bezier | cubicBezier y1/y2 sınırsız — ölçek kapıları keyframe zarfını okuyordu | ✅ YAPILDI | ÖNCE ÖLÇÜLDÜ: editörün easing sözlüğü 4 preset'le sınırlı ve HİÇBİRİ y'yi [0,1] dışına taşırmıyor (ekstremumları tam {0,1}); AMA şema §3.1 y'yi NORMATİF serbest bırakıyor ve ham API'den (0.3,-4,0.6,1) sınıfı eğri keyframe'ler pozitifken örneklenen ölçeği **−1,499**'a indiriyordu (kapı 0.02 görüyordu), (0.3,0,0.6,6) da 2.0'lık keyframe tavanını **3,99**'a taşıyordu. KARAR: şema kelepçesi REDDEDİLDİ (y-serbestlik normatif; elle yazılmış "Özel eğri" belgeleri bugün yasal — kelepçe sözleşme kırardı; overshoot/back/elastic preset sınıfının önünü keserdi) → kapılar EĞRİNİN GERÇEK EKSTREMUMUNU okuyor. Ekstremum KAPALI FORMLA (By′ köklerinden) — yoğun örneklemeye karşı ÖLÇEREK seçildi: 2007 katsayı çiftinde 20k-nokta taramayı DAİMA kapsadı, sapma ≤3,1e-8, ~90 ns vs ~220 µs/segment (~2400×). İKİ DİLDE (mevcut parite-vektör deseni): easing.ts `bezierValueExtrema`/`keyframeCurveExtrema` (REFERANS) ↔ C# `Easing.BezierValueExtrema` + `AnimationTrack.CurveMin/CurveMax`; paylaşılan `easing-extrema-vectors.json` (15 vaka) iki dilde de koşuyor + iki dilde yoğun-tarama kapsama muhafızı (kapalı form operasyonel eğriyi asla daraltamaz). Kapılar: `KeyframeCompiler.Parse`'a eğri-pozitiflik kapısı (keyframe'ler pozitifken eğri ≤ 0 → tipli ret; GateInventory ThrowSiteCounts 8→9 gerekçeli), `MinScaleOf`→CurveMin (degenerate-layer artık eğri tabanından; mesaj eğri &lt; keyframe iken İKİ sayıyı da söylüyor), `PlacementTransform`→CurveMax (transform-scale/bellek tavanı). Preset/linear'da CurveMin/Max == keyframe min/max → editör belgelerinde davranış SIFIR fark (tüm eski snapshot/mesaj testleri değişmeden yeşil; her yeni kapı testinin linear kontrol grubu var). Doküman: rendering-semantics §3.1 normatif serbest-y notu + §2.2 dejenerelik bloğu; opacity/volume aralık kapılarının bilinçli keyframe-değerinde kalışı backlog'a [DÜŞÜK] beyan (sonuç kırık geometri değil — clamp/faz; eğriye taşımak sözleşme kararı ister). NEGATİF KONTROL İKİ DİLDE: C#'ta üç kapı geri alınınca 3 test kırmızı (delik aynen geri geldi: pozitif-keyframe'li undershoot belgesi yine kabul ediliyordu) → md5 birebir geri (`1454538d…0a76` / `bba72bbf…ad82`); TS'te iç-aday döngüsü kaldırılınca 5 test kırmızı → md5 birebir geri (`6508fe2d…18dc`); touch+rebuild sonrası yeşil. NİHAİ KOŞUMLAR (B7+B8+bezier ağacında): build -warnaserror 0/0; backend TAM suite **1557/1557 skip 0** (MinIO+ffmpeg); şema vitest 222/222; editör 1313/1313; `tsc -b` temiz. |
| B6 | Render iptali tek-süreç kapsamından çıktı (reaper ↔ çok-worker borcu) | ✅ YAPILDI | DB satırı SÜREÇLER-ARASI iptal kanalı yapıldı — icat yok, mevcut render yoklaması genişledi: 10 sn'lik cancel yoklaması artık satırın GÜNCEL durumunu okur (AYNI tek-satır PK SELECT, sorgu sayısı DEĞİŞMEDİ; ölçüldü dev Postgres 17 / 3 535 satır: PK index 3 buffer, tekil 0,26 ms, ısınmış 5-10 µs/sorgu ×3 koşum; frekans export WorkerCount=1 ile ≤0,1 sorgu/sn), Running değilse SAHİBİ süreç ffmpeg ağacını öldürür ve satırı yazanın gerekçesiyle bırakır — rethrow YOK (Hangfire retry'ı `stalled` satırı Running'e çevirip işi diriltirdi). Reaper kararını ÖNCE commit eder SONRA öldürür (ters sıra aynı dirilme; `Reaper_StalledJob_CommitsTheDbVerdictBeforeKillingTheRender`); gerekçe ailesi değişmedi (`stalled`/`canceled` aynen → GateInventory defterine dokunulmadı). Registry süreç içi ANINDA ulaşım + hiç-progress halleri için duruyor; kalan sınır (progress üretmeyen kaçak başka süreçte / öksüz ffmpeg = OS süpürmesi) §4.1 + xmldoc'ta beyan. Kanıt (gerçek MinIO+ffmpeg, `ExportJobPipelineTests` orta-render ailesi): BOŞ-registry'li reaper ("başka worker") yalnız DB yazdı → pid GERÇEKTEN düştü, satır `Failed('stalled')` KALDI, exports'a obje çıkmadı; YANLIŞ-ÖLDÜRME AVI: her callback'te bir yoklamayla (saldırgan saat) dokunulmamış satır Succeeded; orta-render KULLANICI iptali (Canceled + 'canceled' stage) ve Hangfire shutdown'ı (Running → rethrow/requeue) ayrı ayrı — orta-render iptalin daha önce HİÇ ürün-yolu testi yoktu. Negatif kontrol: yoklama Canceled-only'ye çekildi → çok-worker testi KIRMIZI (render tamamlanıp `stalled` kararı EZİLDİ) → md5 birebir geri (touch+rebuild) → yeşil. Backlog kaydı KAPANDI; §4.1 iki-yol tablosuna çevrildi. Build -warnaserror 0/0. Nihai tam suite backend 1517/1517 skip 0 (B2'nin HENÜZ COMMIT'LENMEMİŞ `MediaPipelinePerfTests`'i dahil — o dosyaya dokunulmadı ve commit'e alınmadı; İLK tam-suite koşumunda o test paralel yük altında kendi oran eşiğinde bir kez kırmızıydı, aynı ağaçta izole 46 sn yeşil + nihai koşumda yeşil: B2'nin kalibrasyon meselesi, bu dilimin dosyalarıyla ilişkisiz). |

## Küçükler (2026-08-31 — STATE §Sıradakiler madde 5, dört iş TEK commit)

| # | İş | Durum | Not |
|---|---|---|---|
| K1 | SaveTimeline 409 birim sözleşme pini | ✅ YAPILDI | Önce ölçüldü: pin yoktu (DURUM §5 riski), dal yalnız e2e'de korunuyordu. `SaveTimelineRevisionContractTests` (TimelineRequestValidationTests.cs içinde; CrossUserAccessTests'in Sqlite in-memory + doğrudan-handler deseni): doğru baseRevision → 200 + revision tam 1 artar + doküman yazılır; BAYAT baseRevision → 409 + gövde GÜNCEL doküman ve GÜNCEL revision (istemcinin çakışma diyaloğunun okuduğu şekil) + kurban dokümanı/revision'ı DEĞİŞMEDİ + snapshot satırı doğmadı; İLERİDEN gelen baseRevision da 409 (filtrenin `==` olduğu pinli, `<=` değil). CANLI eş-kanıt (ham curl, demo hesabı): PUT#1 rev0 → 200 `{"revisionNumber":1}`; PUT#2 rev0 (farklı gövde) → 409 + gövdede v1 doküman ve revision 1 (v2 sızmadı); prova projesi DELETE 204. Negatif kontrol: ExecuteUpdate WHERE'inden `RevisionNumber == BaseRevision` çıkarıldı → 2 test kırmızı ("bayat yazma Ok'landı" — kaybolan-güncelleme imzası, Expected Conflict / Actual Ok) → md5 birebir geri (`DA038EE6…2949`) + touch+rebuild → 3/3 yeşil. |
| K2 | "Sonradan tekrar düzenleme" gerçek-medya e2e'si pakete | ✅ YAPILDI | 12. turda ölçülmüş ama korunmayan akış (backlog açık borcu) `e2e/relogin-reopen.spec.ts` olarak pakete alındı: kurulum REST'ten (apiUpload başlığındaki sözleşme — iddia dosya seçici değil), akışın tamamı gerçek fare/klavye: gerçek 4 sn'lik medyalı projede sağ tık → "Playhead'de böl" → "Kaydedildi" rozeti → rozetin SÖZÜ sunucu dokümanından ayrıca doğrulanır (2 klip yazılmış) → seçiciden "Çıkış yap" → gerçek klavyeyle yeniden giriş (429-toleranslı, auth.spec deseni) → seçici satırına tıkla → AYNI belge (store klipleri == kaydedilen sunucu klipleri; sunucu dokümanı + revision birebir — açmak salt-okur) + sayfanın KENDİ media-urls isteği 200 + proxy'ye Range GET 206 (`bytes 0-1023/…`, 1024 bayt). Oturum AYRI context'te — logout per-device, paylaşılan worker oturumu düşmez. Negatif kontrol: autosave `putTimeline` yalancı başarıya çevrildi → '"Kaydedildi" göründü ama bölme sunucu dokümanına YAZILMAMIŞ — rozet yalan söylüyor' kırmızısı (Expected 2 / Received 1) → md5 birebir geri (`1D6C03BE…5BEA`) → yeşil (9,6 sn). Backlog 12. tur kaydı + poc §4.2/§5.1 KAPANDI olarak işlendi. |
| K3 | README senkronu | ✅ YAPILDI | Test sayıları 2026-08-31 kendi koşumlarımdan (aşağıdaki suite satırı; MinIO'suz koşum ayrıca ölçüldü: 1519 + 41 atlandı — bayat "17" düzeltildi); mimari şemadaki Redis/SignalR satırına dürüst not (ilerleme yoklamayla; SignalR yazılmadı; ŞEMA KORUNDU — getir/sök kararı kullanıcının, STATE açık soruları); yol haritasında pis-dosya korpusu (DirtyMediaCorpusTests: VFR/döndürülmüş/HLG/dikey uçtan uca — kapsamı doğrulandı) ve track sıralama/adlandırma (track-manage.spec.ts) KAPANDI işaretlendi; özellik listesindeki bayat "track sıralama henüz yok" cümlesi düzeltildi; Proje Yapısı'ndaki 1232 → 1560. |
| K4 | `compose.dev.yml:1` bayat yorumu | ✅ YAPILDI | "SADECE altyapi servisleri (Postgres + Redis)" → "(Postgres + Redis + MinIO)" — dosya 41. satırından beri MinIO da tanımlıyordu. |

Suite (tümü kendi koşumum, 2026-08-31; API/Worker `api-run-kucukler`/`worker-run-kucukler` yayını, tazelik yüklü modül yolu + 3 iğneyle kanıtlı): build -warnaserror 0/0 · backend TAM **1560/1560 skip 0** (MinIO+ffmpeg, 2 dk 16 sn; MinIO'suz 1519 + 41 skip) · şema 222/222 · editör 1313/1313 · `tsc -b` + e2e tsc temiz · prod build ✓ · Playwright TAM **160/160** (10,7 dk; relogin-reopen dahil). Kaçak ffmpeg yok; migration yok; canlı prova artıkları ürün yolundan silindi (DELETE 204).

## SignalR ilerleme kanalı (2026-08-31 — STATE eski §Sıradakiler madde 3, kullanıcı kararı: GETİR)

| # | İş | Durum | Not |
|---|---|---|---|
| S1 | Worker → Redis publish + API forwarder + `JobProgressHub` + istemci (tasarım 03 §5) | ✅ YAPILDI | Önce ölçülen taban: ilerleme yalnız yoklamayla akıyordu (poc §0.1 "UI yoklaması" bileşeni; README dürüst "yazılmadı" notu). Uygulama: kanal sabitleri TEK yerde `Contracts/JobProgress.cs` (`job-progress` / `/hubs/progress` / `progress` / `job:{id}` / `asset:{id}`); worker her progress DB yazımının YANINDA publish (`JobProgressWriter` + Export/ProcessAsset terminal-geçiş yazımları + Hangfire failure filtresi; DB %5-adım throttle'ı ve polling DEĞİŞMEDİ); API'de `RedisProgressForwarder` (düz pub/sub — backplane paketi BİLİNÇLİ yok, tek instance; DECISIONS geri-alma koşulu) grup yayını; istemci `entities/progressHub.ts` (katkı-modelli abonelik, kapsama + 15 sn sessizlik bekçisi, hub kapsarken yoklama durur, aksi her durumda bugünkü 2 sn/3 sn polling YEDEK). JWT query-string YALNIZ hub yolunda (`OnMessageReceived` + Serilog path'i query'siz — redact). Kanıt (hepsi kendi koşumum): e2e `export-progress-hub.spec.ts` — gerçek fare + gerçek WS; hub'dan running→succeeded aktı, canlı pencerede export GET'i 0, uçtan uca ≤4; `progress-fallback.spec.ts` — `/hubs/**` engelli, WS 0, polling ≥2 GET'le iş "Tamamlandı" + indirme 200. Dayanıklılık: Redis container DURDURULDU → koşan API `/health` 200 + login 200; Redis KAPALIYKEN soğuk açılış da 200 + forwarder artan aralıkla dener (log: "retrying in 5s → 10s… API stays up; clients keep polling") ve Redis dönünce KENDİLİĞİNDEN abone olur ("subscribed" INF satırı) — Redis zorunlu değil, üçü de canlıda ölçüldü; in-process `ProgressHubTests` (forwarder iletimi, bozuk payload atlanır, Redis'siz/erişilemez açılış fault'suz, tel sözleşmesi API DTO'suyla enum-tam eşit). NEGATİF KONTROL ×2: forwarder söküldü → e2e "Hub'dan hiç export progress mesajı gelmedi" kırmızısı → md5 `EFC1…2D38` birebir geri → yeşil (5,3 sn). Kapı için S2. |
| S2 | Hub aboneliğinde sahiplik kapısı (IDOR matrisinin hub'a uzantısı) | ✅ YAPILDI | Abonelik REST'le AYNI filtreyi sorar (`Jobs.RequestedBy == userId`; asset için `OwnerId` + soft-delete); ret REST 404'ünün aynası (tek cümle, varlık sızdırmaz, gruba TEK AddToGroup çağrısı bile gitmeden). `CrossUserAccessTests.Hub_SubscribeJob/SubscribeAsset_Foreign*` (saldırgan → HubException + `groups.Added` boş) + pozitif yarım `ProgressHubTests` (sahip doğru gruba girer). Uç envanteri muhafızı GEREKÇELİ genişletildi: `MapHub` Program.cs'te DEĞİL `ProgressHubEndpoints.MapProgressHubEndpoints`'te (satır-içi rota yasağı yaşıyor; muhafızın rota tablosu kurulumuna GERÇEK `AddSignalR` eklendi — hub uçları `* /hubs/progress` + `/negotiate` defter satırlarıyla kapı testlerine bağlandı). NEGATİF KONTROL: `if (!owns)` kapısı söküldü → `Hub_SubscribeJob_ForeignJob…` "No exception was thrown / Expected HubException" kırmızısı → md5 `BF37…3B90` birebir geri → 2/2 yeşil. |

Suite (tümü kendi koşumum, 2026-08-31; API/Worker `api-run-signalr`/`worker-run-signalr` yayını, tazelik yüklü modül yolu + 6 iğneyle kanıtlı): build -warnaserror 0/0 · backend TAM **1575/1575 skip 0** (MinIO+ffmpeg, 2 dk 7 sn) · şema 222/222 · editör 1324/1324 · `tsc -b` + e2e tsc temiz · Playwright TAM **162/162**. Kaçak ffmpeg yok; migration yok.

## Ortam notları

- 2026-08-22: Docker Desktop kapanmış bulundu; kaldırıldıktan sonra taban çizgisi
  1415/1415 yeşil doğrulandı (dünkü 31 kırmızı ortamsaldı).
- 2026-08-24 (#3 sonrası): servisler scratchpad `api-run26` / `worker-run26` (Release)
  altından koşuyor; yüklü modüller + çift-hizalamalı iğne taraması ile doğrulandı.
- 2026-08-24 (#4 sonrası): servisler `api-run27` / `worker-run27` (Release) çiftine alındı
  (yüklü modül yolları doğrulandı); `AddAssetFilmstripManifest` migration'ı canlı DB'ye
  `--migrate-only` ile uygulandı (`__EFMigrationsHistory` son satırı + jsonb kolon sorgusuyla
  doğrulandı). Not: migration yalnız `--migrate-only` bayrağıyla koşar — servis restart'ı
  tek başına şemayı GÜNCELLEMEZ.
- 2026-08-24 (#5 sonrası): servisler `api-run28` / `worker-run28` (Release, blend yerli mod)
  çiftine alındı; yüklü modül yolları + çift-hizalamalı UTF-16 iğne taraması ile doğrulandı
  (`blend=all_mode=normal:all_opacity=` VAR, eski `blend=all_expr='A*(1-` YOK). Migration yok.
- 2026-08-25 (nihai kapanış doğrulaması): 9 madde tek koşumda yeniden kanıtlandı. Servisler
  HEAD (384cf6d) `dotnet publish` çıktısı `api-run-final` / `worker-run-final` (Release)
  çiftine alındı; yüklü modül yolları + çift-hizalamalı UTF-16 iğneyle tazelik doğrulandı
  (`VideoEdit.Media.dll`'de yeni `blend=all_mode=normal:all_opacity=` VAR, eski
  `blend=all_expr='A*(1-` YOK; `VideoEdit.Api.dll`'deki tek `all_expr=` vuruşu davranış
  değil, `ClipEffects.LutBlendFilter` XML doküman yorumunun literalidir). Vite taze
  başlatıldı; kaçak ffmpeg yok. Kanıtlar: Playwright TAM suite 157/157 yeşil (581 sn, tek
  kırmızı yok); demo senaryosu gerçek fare/klavyeyle uçtan uca (export tamamlandı, indirilen
  MP4 ffprobe'da h264+aac 18 sn, müzik 440 Hz imzası içeride 0 dB / dışarıda −86,7 dB);
  #3 canlı export comp-1080p + comp-2160p bileşim `succeeded`, fiziksel-boş 4,41 GiB iken
  bile `memory-wait`/`insufficient-memory` ÜRETİLMEDİ (commit-boşluk kapısı doğru);
  #4 yeni asset media-urls p50 2,55 ms + DB `FilmstripManifest` jsonb DOLU (canlıda 97 hızlı
  yol / 2069 yedek yol); #5 comp-1080p 60 sn belge 1,58x & 1,52x (yakalanan graph'ta
  `all_opacity=0.2` + `lut3d`, `all_expr` YOK), LUT'lu export ffprobe'da 1920×1080 h264;
  #6 cross-user 3 uç (proje + media-urls + asset) 404, kurban durumu değişmedi;
  #12 kasıtlı CS1573 sade `dotnet build`'i kırdı (exit 1), geri alınınca 0/0;
  #13 `/health` fonts parmak izi `f8620403…c1f4861d` == worker açılış satırı; #1/#2/#14 suite
  yeşilliğiyle + izole koşumla sabit. `git status` PROGRESS.md dışında temiz. Migration yok.
- 2026-08-25 (B-kenar/lut öncesi): `api-run-b2`/`worker-run-b2` HEAD'den eskiydi; servisler
  HEAD'in (395c246; backend'i 3a1c256 ile birebir) TEMİZ KLONUNDAN yayınlanan
  `api-run-b4lut`/`worker-run-b4lut` (Release) çiftine alındı. Yayının temiz klondan olması
  bilinçli: çalışma ağacında o sırada BAŞKA bir oturumun commit'lenmemiş worker değişiklikleri
  (ExportEstimateOptions WIP) belirmişti — canlıya sızmadığı, yüklü Worker.dll'de
  `ExportEstimateOptions` UTF-8 iğnesinin YOKLUĞUyla; tazelik, yüklü modül yolları +
  Media.dll'de `blend=all_mode=normal:all_opacity=` (UTF-16) ve `ExpectedOutputClockUs`
  (UTF-8 tanımlayıcı — B5) iğnelerinin VARLIĞIYLA kanıtlandı. İğne dersi: tanımlayıcı adları
  metadata'da UTF-8'dir, UTF-16 taraması yalnız dizge literallerini görür. Migration yok.
- 2026-08-25 (B2 canlı ölçümü öncesi): `api-run-final`/`worker-run-final` HEAD'den ESKİYDİ
  (yüklü `VideoEdit.Media.dll`'de B5 iğnesi `ExpectedOutputClockUs` YOKTU — çift-hizalamalı
  taramayla ölçüldü); servisler güncel ağaçtan yayınlanan `api-run-b2`/`worker-run-b2`
  (Release) çiftine alındı ve tazelik aynı iğnenin VARLIĞIYLA kanıtlandı. Not: yayın anındaki
  ağaç HEAD (935a73c) + B6 diliminin commit'lenmemiş worker iptal-kanalı değişikliğiydi
  (mutlu-yol perf davranışını değiştirmiyor; B2'nin §0.1.1 kaydında beyanlı). Migration yok.
- 2026-08-25 (B borçları KAPANIŞ doğrulaması): dokuz borç tek koşumda canlı/kendi-koşum
  kanıtlarıyla yeniden doğrulandı. Servisler HEAD (780791f) `dotnet publish` çıktısı
  `api-run-bfinal` / `worker-run-bfinal` (Release) çiftine alındı (eski b4lut çifti 3 commit
  geriydi: B7/B8/bezier canlıda değildi); tazelik yüklü modül yolları + iğnelerle:
  Worker.dll'de `ExportEstimateOptions` (UTF-8, B7) ve Media.dll'de `BezierValueExtrema`
  (UTF-8, bezier) VAR — b4lut ikilisinde İKİSİ DE YOKTU — Media.dll'de
  `blend=all_mode=normal:all_opacity=` (UTF-16) VAR. timeline-schema dist'i 21.08'den
  BAYATTI (dist/easing.js'te bezier iğnesi yoktu; editör paketi dist'ten çözer) → `pnpm
  --filter @videoedit/timeline-schema build` + Vite taze başlatıldı; Vite'ın /@fs ile servis
  ettiği easing.js'te `bezierValueExtrema` doğrulandı. Suite'ler (tümü kendi koşumum):
  build -warnaserror 0/0; backend TAM **1557/1557 skip 0** (MinIO+ffmpeg, 2 dk 13 sn); şema
  222/222; editör 1313/1313; `tsc -b` temiz; Playwright TAM **159/159** (10,6 dk; tek kırmızı
  yok — B1 audio-parity, lut.spec 3/3 ve Ctrl+D/V grid bu koşumun içinde). Borç kanıtları:
  B5 yalan-süre m4a (beyan 2 sn / gerçek akış 60 sn, mvhd/tkhd/mdhd yamalı) canlı yükleme
  akışından geçti → asset 2,0 sn'de failed, `errorCode: transcode-overrun`; B6 canlı 2160p
  render %20'deyken satır psql'den (süreç-dışı kanal) Failed('stalled')'a çekildi → ffmpeg
  flip+5,5 sn'de öldü (yoklama ≤10 sn), satır YAZANIN gerekçesiyle kaldı (OutputKey boş,
  AttemptCount 1, 25 sn'de dirilme yok; worker logu "finalized externally… row left as
  written"), dokunulmamış satırlı normal render'lar kesintisiz succeeded; B2
  MediaPipelinePerfTests izole 46 sn yeşil + canlı init tam 4 GiB → 201/partCount 64 (abort
  edildi), 4 GiB+1 → 400 (10 ms, "sizeBytes must be between 1 and 4294967296"); B4 ci.yml
  yorumu sayısız hâliyle yerinde (mekanizma + sayının bilinçli yokluğu, satır 25-34); B7
  worker açılış satırı varsayılanlarla "…taban=268435456 B… ezilen: yok",
  `ExportEstimates__MemoryBaseBytes=536870912` ile yeniden başlatınca "taban=536870912 B…
  ezilen: MemoryBaseBytes" (yalnız o terim değişti), temiz yeniden başlatışta varsayılanlara
  döndü; B8 eski (kolon NULL) asset'li 21.08 projesinde 1. media-urls 69,7 ms → İKİ video
  asset'inin kolonu 121 B jsonb DOLDU (audio bilinçli NULL kaldı), 2..6. çağrılar p50 5,2 ms
  — yedek yol yalnız kolon NULL iken koştuğundan ikinci çağrı DB'den, yanıt şekli birebir;
  B-kenar/bezier ham API undershoot belgesi (cubicBezier 0.3,-4,0.6,1; keyframe'ler POZİTİF
  0.02..1) → SENKRON 422 (194 ms) "örneklenen eğrinin en küçük değeri -1.5", kuyruğa iş
  düşmedi; eski (linear/preset) ADV10 belgesi yeniden export → succeeded 5,8 sn (davranış
  değişmedi). Önceki tur örneklemi (3): miks-asılması belgesi (19 sn, klip B kaynağın
  SONUNA demirli çift sesli grup) canlı succeeded 5,05 sn + kaçak ffmpeg 0; render-overrun
  OutputTimeCeiling ailesi 4/4 (gerçek kaçak süreç 617 ms'de öldürüldü + normal transcode
  yanlış öldürülmedi); Ctrl+D/V grid (timeline-gates "3'e bölünmeyen kare sayılı kopya")
  Playwright TAM koşumda yeşil. B6 metod notu: 1080p'lik İLK deneme yarışı ölçtü — 80 sn'lik
  belge ~13 sn'de render olunca flip'ten sonra render 10 sn'lik yoklamadan ÖNCE doğal bitti
  ve satırı Succeeded ile ezdi; bu pencere üretimde reaper'la açılamaz (reaper yalnız 6 saat
  kalp atışsız satırı çevirir — canlı progress'li satıra hiç dokunmaz), kanıt geniş
  pencereli 2160p ile kuruldu. Prob artıkları ürün yolundan temizlendi (3 asset + 6 proje
  DELETE 204; 2 prob export objesi mc rm; prob Jobs satırları silindi; ADV10'un yeniden-koşum
  çıktısı canlı projenin gerçek geçmişi olarak bırakıldı). Kaçak ffmpeg yok; worktree tek;
  `git status` PROGRESS.md dışında temiz. Migration yok.

## Yarım-iş turu 2 (2026-08-31 onayı: #1 defter, #6 catch envanteri, #5 B6 senkron)

| # | İş | Durum | Not |
|---|---|---|---|
| 1 | Bayat defter kayıtları (BG-4/BG-5/M3-1109) | ✅ YAPILDI | Üçü de kodda zaten kapalıydı (grep kanıtları: feedbackCoverage.test var + eski eşsiz reason'lar feedback.ts'te; hasClipboardContent 0 geçiş; 6 parite vektör dosyası mevcut) — backlog kayıtları KAPANDI işlendi, tarihsel metin korundu. |
| 6 | Boş catch envanteri | ✅ YAPILDI | ÖNCE ÖLÇÜLDÜ: DURUM'un "8+" tahmini yerine TAM envanter mekanik taramayla: **35 sessiz catch** (26 blok + 9 promise; `apps/editor/src` — `packages/timeline-schema/src`'de 0 catch), elle sınıflandırma taramayla birebir; abort görünmezliği kendim üretildi (abortUpload reddi sıfır console izi — 3 yeni test önce KIRMIZI). Sınıflandırma: 19'u gerekçeli-geçerli (davranışa DOKUNULMADI); 12 gerekçesiz/vakum-yorumlu siteye tek satır gerekçe (auth×4, editorStore, progressHub stop, audioGraph close, fontCatalogue×2, engineV1×3 — davranış sıfır, çoğu teardown/fallback sınıfı); 4 abort-başarısızlığı sitesi (uploadEngine init-yarışı/iptal/complete-uçuşta + LibraryPanel discard) yeni `lib/devWarn` ile dev-only görünür — üretimde BİLİNÇLİ sessiz (kullanıcının eyleyebileceği şey yok, 7 günlük lifecycle süpürür; refuseWhenLocked/assertDocGateDev "dev'de gürültülü, prod'da sessiz" kapı deseni), kullanıcı görünür davranış değişmedi (sonuç yine 'aborted', hata kartı yok). Testler: `devWarn.test.ts` 3 (önek+err iletimi / tek argüman / DEV=false SESSİZ) + uploadEngine'e 3 abort-reddi testi (üç yol da: yutma bozulmadı + dev-warn düştü). KALICI MUHAFIZ (feedbackCoverage deseni): `silentCatchInventory.test.ts` — blok+promise sessiz formlarını kaynak taramasıyla bulur, HER sessiz catch'e gerekçe-yorum ZORUNLU (yorumsuz eklenemez), taban sayılar (editör ≥100 dosya / şema ≥4 / sessiz ≥25; bugün 31 — 4 abort sitesi devWarn'la "işlenmiş" sınıfına geçti) + 3 bilinen site çivili; şema kökü de taranıyor. NEGATİF KONTROL ×2: devWarn(iptal yolu) söküldü → 'mid-upload cancel' testi "expected [] to have a length of 1 but got +0" KIRMIZI → geri → md5 `8593ab37…9887` birebir; editorStore gerekçe yorumu söküldü → muhafız ihlali TAM adresle ("state/editorStore.ts:82 (block)") KIRMIZI → geri → md5 `2b9011e1…36f2` birebir. Suite: build -warnaserror 0/0 · backend TAM **1575/1575 skip 0** (MinIO+ffmpeg) · editör **1333** (+9) · şema 222 · tsc -b + e2e tsc temiz · prod build ✓ · Playwright media-upload/library/library-manage **7/7** (gerçek fare — dokunulan alanda davranış değişmedi kanıtı; API `api-run-verify`, tazelik JobProgressHub/RedisProgressForwarder iğneleriyle). |
| 5 | B6 çapraz-sekme kitaplık senkronu (user-feed) | ✅ YAPILDI | ÖNCE ÖLÇÜLDÜ (üç katman): (a) kusur UI'da yeniden üretildi — yeni spec HEAD'de KIRMIZI (pasif sekme, ham API'den yüklenen asseti 30 sn'de görmedi; orijinal 192→194 ölçümünün aynası); (b) telde kanıt: bugünkü payload'da sahip alanı YOK, abonelik id-bazlı — pasif sekmeye ulaşacak grup adreslenemiyor; (c) yayın kaynağı tasarım ölçümü: complete→ilk worker publish n=3 = 1,79/0,62/0,74 s (p50 0,74; Hangfire pickup, B2'nin 0,6 sn kuyruk ölçümüyle tutarlı) → WORKER-PUBLISH YETERLİ, API'ye ikinci Redis publish yönü AÇILMADI (DECISIONS satırı, reddedilenlerle). ÇÖZÜM (en küçük tutarlı): Contracts `JobProgressMessage.OwnerId` (null=eski payload → feed atlanır; `Forwarder_LegacyPayload…` pini) + `UserGroup` sabiti; worker `FromJob` RequestedBy'ı taşır; forwarder sahipli mesajı `user:{id}` grubuna DA yollar; hub'a PARAMETRESİZ `SubscribeUserFeed`/`UnsubscribeUserFeed` — kimlik JWT'den, başkasının feed'i hub yüzeyinde İFADE EDİLEMEZ (IDOR aynası `Hub_SubscribeUserFeed_LandsInTheCallersOwnFeed_NeverTheVictims`; DB turu yok). İstemci: `useProjectAssets` mount'ta feed katkısı (katkı modeli; bağlantı meşgul satır olmadan da yaşar, restart kapısı feed'i sayar; feed `covered`/bekçi DIŞINDA — sessizliği normaldir, kapatacağı polling kapısı yok, kurulamazsa SignalR-öncesi davranışa düşer); `handleProgressMessage` üç katman: ardışık kopya-teslim süzgeci (aynı mesaj asset+feed grubundan çift gelir — çift terminal invalidate önlenir) → bilinmeyen-asset tepkisi (listede olmayan assetId: liste+kota BİR kez invalidate; noticed seti sonraki %5-adımları süzer; terminalde liste invalidate'i applyAssetMessage'ın terminal dalına bırakılır — tek refetch) → mevcut `applyProgressMessage` DEĞİŞMEDİ. FIRTINA YOK kanıtı: canlı koşumda upload→Hazır TOPLAM 2 liste GET'i. KANIT: `e2e/library-crosstab-sync.spec.ts` — pasif sekme gerçek girişle açık, yükleme HAM API'den (başka istemci), sekmeye tek jest yok + `page.on('load')` sayacı 0; sessiz pencere 0 liste GET'i (kusur mekanizması hâlâ yerinde); satır 2 846 ms'de + "Hazır" + kota göstergesi kendiliğinden; ilk liste GET'i ilk feed mesajından SONRA (tetikleyici hub, ilk feed +2 577 ms). Birim: +5 vitest (novelty bir-kez/bilinen-id-atlanır/export-kota-yok/terminal-tek-liste/kopya-süzgeci) + feed katkı modeli; backend +4 (feed üyeliği pozitif, forwarder 3-grup, eski-payload feed atlanır, tel `ownerId` camelCase pini) + IDOR aynası. NEGATİF KONTROL ×3 (+HEAD kırmızısı zaten ölçüm): istemci `SubscribeUserFeed` invoke'u söküldü → spec "Pasif sekme … görmedi (B6 kusuru açık)" KIRMIZI → md5 `16F70F6C…68B8` birebir; forwarder feed bacağı söküldü → `Expected: 3 / Actual: 2` KIRMIZI → md5 `DA78F705…30CF` birebir + rebuild 7/7; novelty çağrısı söküldü → 2 vitest KIRMIZI (çağrı-sayısı imzaları) → md5 birebir → 17/17. Suite (kendi koşumlarım): build -warnaserror 0/0 · backend TAM **1579/1579 skip 0** (MinIO+ffmpeg) · şema 222 · editör **1339** (+6) · tsc -b + e2e tsc + prod build ✓ · Playwright TAM **163/163** (progress-fallback + export-progress-hub + media-upload/library komşuları dahil). Backlog M2/B6 kaydı KAPANDI. |

## Geliştirme turu 3 (2026-08-31 onayı: #1 defter senkronu, #2 silme+retention+e2e temizliği, #3 export perf 2. tur)

> Kapsam 2026-08-31 keşif turundan (4 paralel salt-okunur tarayıcı) çıkan listeden kullanıcı
> onayıyla seçildi. Seçilmeyenler (muhafız/CI sertleştirme, küçük kod sağlamlaştırmaları,
> #9/#10/#11, kural görünürlüğü, C#→TS üretimi, dağıtım sertleştirme) backlog'da kalır.
> SkiaSharp penceresi + import-yönü kuralı: kullanıcı "başka bir şey" dedi, ayrıntı bekleniyor.

| # | İş | Durum | Not |
|---|---|---|---|
| 1 | Defter/doküman senkronu (bayatlık sınıfı) | ✅ YAPILDI | DAVRANIŞ SIFIR (yalnız doküman + kod yorumları); 11 keşif tespitinin 11'i de kendi doğrulamamla TUTTU, hiçbiri atlanmadı. Eski↔gerçek çiftleri: (1) README "1560/1313/160 + relogin-reopen en son" ↔ STATE yeşili 1579/1339/163 + son eklenen library-crosstab-sync (UnitTests satırı "güncel sayılar: docs/STATE.md" işaretine çevrildi; golden 11+4 PNG sayıları doğrulandı, dokunulmadı); (2) backlog "[AÇIK] ci.yml doğrulanamayan sayı" ↔ ci.yml:25-34 B4'te (`3a1c256`) sayısızlaştırılmış — KAPANDI; (3) backlog "SignalR gelince refetchIntervalInBackground kaldırılacak" ↔ SignalR geldi AMA polling bilinçli YEDEK (DECISIONS) + bayrak assets.ts:79/exports.ts:143,171'de duruyor (grep) — "kaldırılMAYACAK" kararıyla KAPANDI; (4) backlog 503 font-kökü "ölçülmedi" ↔ FontRootHealth (yarim-is #13) iki uca aynı parmak izini verdiriyor — kayıt "işletmeci karşılaştırması kaldı"ya daraltıldı; (5) backlog fitButton bayat-locator "Yapılacak" ↔ editor.ts:76-78'de `/^(Fit|Sığdır)/i` regex'i `37a11b1`'de uygulanmış (git show ile doğrulandı) — KAPANDI; (6) backlog "bayat-dist prosedürü yazılmalı" ↔ SKILLS "schema-dist-tazeleme" girdisi prosedürü içeriyor — KAPANDI; (7) poc §3.2 "korpus test edilmedi" ↔ DirtyMediaCorpusTests+ProcessAssetPipelineTests gerçeği — başlık "SENTETİK test edildi; gerçek telefon dosyası denenmedi"ye döndü; (8) ci.yml "~9 dk (94 test)" ↔ doğrulanmamış-sayı sınıfı — sayısızlaştırıldı (B4 deseni, işaret SKILLS playwright-tam-suite; timeout DOKUNULMADI); (9) MinioSmoke "CI'da koşmaz" ↔ ci.yml dotnet job MINIO_AVAILABLE=1 set ediyor — xmldoc düzeltildi; (10) PlayerPanel:220+editorBridge "until the store carries userSeekSeq" ↔ editorStore.ts:35 tipte + :102'de 0 ile ilkleniyor — yorumlar savunma-dalı gerçeğine çevrildi, KOD DEĞİŞMEDİ; (11) deploy/README damgası 2026-08-12 ↔ içerik hâlâ doğru — damga 2026-08-31 teyidiyle tazelendi. EK BULGU: ci.yml e2e job'unun redis yorumu "backend'de tek tüketici yok" diyordu — SignalR sonrası YANLIŞ (11 dosya grep'i); yorum düzeltildi, job'un redis başlatmayışı (export-progress-hub.spec Redis bekler) davranış işi olarak STATE "Bilinen sorunlar"a + ayrı görev fişine. B6 kapsam-dışılık kaydı bilerek YAZILMADI (tur 3 #2 anında bayatlatırdı). Kapılar (kendi koşumum): build -warnaserror 0/0 · backend TAM **1579/1579 skip 0** (MinIO+ffmpeg, 2 dk 11 s) · editör **1339** + şema 222 · tsc -b + e2e tsc temiz. Playwright GEREKMEDİ (davranış değişikliği yok — beyan); NEGATİF KONTROL bu dilime UYGULANAMAZ (davranış yok — beyan), yerine yukarıdaki 11+1 eski↔gerçek çifti. |
| 2 | Silme senkronu + revision retention + e2e hesap temizliği | ✅ YAPILDI | **2a (silme çapraz-sekme senkronu) BİTTİ.** ÖNCE ÖLÇÜLDÜ: yeni `e2e/library-crosstab-delete.spec.ts` taze HEAD yayınında KIRMIZI (pasif sekme, başka istemcinin ham-API DELETE ile sildiği asseti 30 sn boyunca göstermeye devam etti — 62 locator çözümü, satır hep 1; SoftDelete yalnız DB ExecuteUpdate+204, hiçbir yayın yok). ÇÖZÜM (en küçük tutarlı; DECISIONS satırı): Contracts'ta TEK tanım `JobProgressChannel.HubMethodAssetRemoved`("assetRemoved") + `AssetRemovedMessage(AssetId)` (iş-şekilli mesaja sahte status bindirilmedi); API soft-delete sonrası sahibinin `user:{id}` feed grubuna SÜREÇ-İÇİ IHubContext gönderimi (Redis turu yok — olay hub'la aynı süreçte; yalnız etkilenen-satır=1'de, best-effort, idempotent 2. DELETE olaysız); istemci (`handleAssetRemovedMessage`) satırı liste cache'lerinden CERRAHİYLE düşürür + YALNIZ kotayı invalidate eder + id'yi noticed setine yazar (geç worker mesajı diriltemez). Kapsam sınırı korundu: timeline missing-media davranışına dokunulmadı (library-manage 3/3 yeşil). KANIT: spec yeşil — satır **33 ms**'de odak/yenilemesiz düştü, ilk assetRemoved çerçevesi +21 ms, silme sonrası liste GET **0**, kota GET çerçeveden SONRA, `page.on('load')` 0; IDOR aynası birim: yayın YALNIZ sahibinin feed grubuna (`SoftDelete_PublishesAssetRemovedOnlyToTheOwnersFeedGroup`; yabancı 404 yolunda `hub.Sent` boş); eski davranış yedeği: WS engelliyken progress-fallback + export-progress-hub + library/media-upload komşuları 10/10 yeşil. NEGATİF KONTROL ×2: istemci dinleyicisi söküldü → spec "silme-senkronu kusuru açık" KIRMIZI → geri → md5 `A26654DD…4EA1` birebir; sunucu yayını söküldü → birim "Assert.Single() Failure: The collection was empty" KIRMIZI → geri → md5 `E7B67225…993B` birebir + rebuild yeşil. Kapılar (bu dilim): build -warnaserror 0/0 · backend TAM **1581/1581 skip 0** (MinIO+ffmpeg) · editör **1342** (+3) · tsc -b + e2e tsc temiz. **2b (ProjectRevisions retention) BİTTİ.** ÖNCE ÖLÇÜLDÜ: worker'da kayıtlı tek recurring iş `asset-reaper` (Program.cs — tek `AddOrUpdate`); canlı dev DB'de **15 861 Auto revizyon / 29 MB** birikmişti (sınırsız büyüme, poc §4.3 gerçek çıktı; dağılım: proje başına ≤50, binlerce küçük e2e projesi → 2c bağlantısı). ÇÖZÜM: `ProjectRevisionRetentionJob` (AssetReaperJob desenli, [Queue("transcode")], saatlik recurring `revision-retention`) — tasarım 03 tarifinin uygulanan yorumu DECISIONS satırında: proje başına EN YENİ 50 Auto her koşulda + 24 sa'ten GENÇ Auto'lar sayıya takılmaz + eskiler saat kovasına inceltilir (kovayı tutulan kayıt temsil eder); **Checkpoint (adlandırılmış) ve PreRestore (restore'un tek kurtarma yolu — versions UI dayanağı) sorguya hiç girmez + silme sorgusunda ikinci Kind kilidi**; export etkilenmez (Jobs.TimelineSnapshot kendi kopyası — ProjectRevisions'ı okuyan tek yer revisions/restore uçları). Sabitler config (`RevisionRetention__KeepLatestAuto/ThinOlderThan/ThinBucket`; açılışta Validate + etkin-değer logu — bozuk override ilk koşumda geçmişi süpüremez, `Run_InvalidOptions_RefusesLoudly…`); migration GEREKMEDİ (şema değişikliği yok). KANIT: birim 10/10 — sınır çifti (tam 50 dokunmaz / 51.'de en eskiyi süpürür), genç koruması, kova temsilcisi, proje izolasyonu (başka proje/kullanıcı sayılmaz), Sqlite'ta gerçek Run() (53 eski auto→50; Checkpoint+PreRestore KALDI), muhafız `WorkerProgram_RegistersBothRecurringJobs` (Program.cs kaynak taraması — kayıt sökülemez); CANLI: taze worker yayını (`ProjectRevisionRetentionJob` iğnesi + yüklü modül yolu), Hangfire storage'da `recurring-job:revision-retention` satırı, seed 60 eski auto + checkpoint + prerestore → gerçek worker'da tetiklenen iş **10 süpürdü** (kalan: en yeni 50, rev 111-160 + iki korumalı tür; log "10 auto snapshot inceltildi (15871 auto tarandı)"). NEGATİF KONTROL: son-50 koruması söküldü → 4 test KIRMIZI (`Assert.Equal() Failure` sınır+izolasyon imzaları) → geri → md5 `859BBF12…7683` birebir → rebuild → 10/10. Kapılar (bu dilim; frontend'e dokunulmadı): build -warnaserror 0/0 · backend TAM **1591/1591 skip 0** (MinIO+ffmpeg). **2c (e2e hesap temizliği) BİTTİ.** ÖNCE ÖLÇÜLDÜ (kirli durum, DryRun): **946** `e2e-*` hesabı / **15 435** proje / **15 290** revizyon / 2 122 asset / 2 724 job; MinIO media bucket 26 GiB / 12 604 obje (2b'nin 15 861 revizyon gözleminin kaynağı da buydu — dağılım binlerce küçük e2e projesi). ÇÖZÜM: `scripts/cleanup-e2e.ps1` (dev-yönlü, ELLE koşulan; ürüne kullanıcı-silme ucu AÇILMADI, globalTeardown'a BAĞLANMADI — DECISIONS 2c satırı gerekçe+reddedilenlerle): hedef SABİT compose.dev.yml konteynerleri (bağlantı dizesi parametresi YOK + compose-etiket/DB-adı fail-fast'i), aday deseni iki üreticinin birleşimi (`e2e-*`/`e2e-auth-*`), demo ya da desen dışı aday → exit 2 hiçbir şey silinmeden (SQL tarafında da aynı süzgeç — çifte kilit); DB tek transaction (Jobs→Projects[cascade revisions/project_assets]→Assets→Users[cascade identity/refresh]); MinIO `u/{userId}/` + yalnız OutputKey'li işlerin `exports/{projectId}/` prefix'leri (mc, 40'lık gruplar). KANIT (gerçek koşum): 946 hesap + tüm satırları silindi; media bucket 26 GiB/12 604 → **19 GiB/2 228** obje; **demo@videoedit.test BİREBİR DURUYOR** (2.7 GiB/1 021 obje + 225 asset satırı öncesi=sonrası); örnek e2e prefix'i boş; desen dışı 63 eski elle-açılmış hesap bilerek DOKUNULMADI. Temizlik sonrası TAM Playwright koşumu yeşil (aşağıdaki kapı satırı — temizlik koşumları bozmaz). NEGATİF KONTROL: desen `%@videoedit.test`e genişletildi → betik `GUVENLIK: aday listesinde korunan ya da desen dışı hesap var` ile **exit 2**, hiçbir satır silinmedi (users 1010 sabit, demo 1) → geri → md5 `7D45BEF7…3C51` birebir; ayrıca gerçek yaşanan tetik: ilk koşumda `e2e-auth-*` şekli strict-regex'e takıldı ve betik silmeden durdu (koruma yük taşıyor). TUR KAPANIŞ KAPILARI (hepsi bu dilimde, kendi koşumlarım): build -warnaserror 0/0 · backend TAM **1591/1591 skip 0** (MinIO+ffmpeg, 2 dk 10 s) · şema 222 · editör 1342 · tsc -b + e2e tsc temiz · prod build ✓ · Playwright TAM **164/164** (yeni library-crosstab-delete dahil). |
| 3 | Export perf 2. tur (baş mimar sözleşme kararıyla) | ✅ YAPILDI (hedef kararı: ölçüt daraltıldı, KAPANDI — 2026-09-01 kullanıcı) | Üç bacak üç commit (`e364f2a` füzyon, `56abab7` tuval-atlama, `6237fcf` budama) + kapanış. Kabul ölçütü GERÇEKÇİ fixtüre demirlendi (Bacak 0); compReal-1080p aynı-gün zinciri **38,3 → 32,4 s** (bitişik-pencere çiftleri 41,4→35,5→32,3), örtülü sınıf **41,4 → 20,1 s (2,99x)**, dikey 2,10x (ilk ölçüm), nihai matris rapor §12.5. Bayt sözleşmeleri: (c)+(a) çift-varyant canlı golden'lar + iki A/B worker sha256 eşitliği; (b) zarf golden'la çivili (normatife yakınsama). Üç bacakta da negatif kontrol (2/27/5 kırmızı) md5-birebir geri. **Hedef (≥2x compReal-1080p) 1,85x'te KARŞILANMADI** — baş mimar kararı gereği (d) N-paralel AÇILMADAN kullanıcıya soruldu (STATE açık soru 6). Kapanış kapıları (kendi koşumlarım): build -warnaserror 0/0 · backend TAM **1626/1626 skip 0** (MinIO+ffmpeg) · şema 222 · editör 1342 · tsc -b + e2e tsc temiz · prod build ✓ · **Playwright TAM 164/164** (10,4 dk; lut/speed-color/export ailesi dahil — parite eşikleri değişmedi). Bacak ayrıntıları aşağıdaki kayıtlar. **DENETİM (bağımsız baş mimar denetçisi, kendi koşumlarıyla): ONAY** — kapılar + bench (±%1: compReal 32,7 s/1,83x, compCovered 19,8 s/3,03x) + sha256 bayt-aynılık + negatif kontrol tekrarı (5 kırmızı, md5 `842FD84A…9731` birebir) denetçinin KENDİ koşumları; 2 ORTA bulgu (yanlış-etiketli sayı sınıfı) orkestratör tarafından ham kayıtlara karşı doğrulanıp DÜZELTİLDİ: ClipEffects xmldoc "33'ünde HEPSİNDE birebir" → gerçek **24/33 birebir + 9'u ±1 LSB** (ham kayıt `ca-sweep-final.txt` casesExactVsRef=24/33); "rig p50 40,4→33,3" üç konumda (rendering-semantics §4.1, DECISIONS, ClipEffects) → gerçek **p50 41,4→34,4** (ham `timings.jsonl`; 40,43/33,37 İLK TUR değerleriydi — `e364f2a` commit mesajı da aynı yanlış etiketi taşır, tarih yeniden yazılmadı, düzeltme bu not + düzeltme commit'idir). |

**#3 bacak kayıtları (baş mimar kararı: Bacak 0 → (b) → (c) → (a); her bacak ayrı commit):**

- **Bacak 0 — fixtür dürüstlüğü + taban (2026-09-01, ölçüm; kod değişikliği yok).**
  (i) Eski perf fixtürünün kusuru KAYNAĞINDAN doğrulandı (`perf20/lib.mjs compDoc`):
  tracks[0]=video EN ÜSTTE, metin/şekil altında TAMAMEN örtülü — üstelik transform'ları
  (y=-300 / x=500,y=300) normalize aralığın dışında, yani tuval DIŞINDA (çifte dejenere).
  (ii) GERÇEKÇİ varyant (compReal: overlay'ler EN ÜSTTE + tuval içinde y=-0.3 / x=0.25,y=0.3)
  + ÖRTÜLÜ varyant (compCovered: tam-ekran cutaway [20,40] altında metin [22,38] + şekil
  [24,36] + colorAdjust'lı alt-açı video [21,39]; taban run [0,60] tek-run kapsamasına
  GİRMEZ) + dikey varyant (compDikey) ham API'yle kuruldu (şema+grid doğrulamalı).
  (iii) Aynı-gün taban (api/worker-run-g31 = HEAD 989ddd1; 3'er koşum p50 sunucu duvarı):
  comp 38,4/40,9*/51,8 s · compReal **36,2/38,3/47,6 s (720p/1080p/2160p → 1,66x/1,57x/1,26x)**
  · compCovered 36,1/41,4/51,6 s. (*comp-1080p'nin r0'ı 37,8 idi; sonraki koşum penceresi
  ~3 s yavaşladı — bacak sonu önce/sonra çiftleri bu yüzden bitişik pencerede ölçülür.)
  **≥2x kabul ölçütü GERÇEKÇİ fixtüre yeniden demirlendi: hedef compReal-1080p ≤ 30 s.**
- **Bacak 1 — (b) colorAdjust füzyonu ✅ (2026-09-01).** ÖNCE ÖLÇ (çıkar-koş-ölç rig'i:
  compReal'in worker'dan YAKALANAN gerçek grafiği + birebir girdiler + aynı ffmpeg 8.0,
  round-robin 3 tur): taban 40,4/41,4/42,4 → füzyon **33,4/36,4/34,4 s (p50 −7,1 s, CPU
  −80 s)** → eşik (≥2 s) fazlasıyla aşıldı, uygulandı. Uygulama: `ClipEffects.FusedStagesFilter`
  — aşama 1-4 (exposure→temperature→tint→contrast+brightness) kanal başına TEK lutrgb bileşik
  ifadesi; aşama sırası ve aşama-başına clip ifadede KORUNDU, DOUBLE'da değerlendirme + tek
  nihai `round(…)` (ölçümle seçildi: çıplak ifade lutrgb'nin `(int)` kesmesiyle normatif
  referansa ±1 bırakıyordu; round ile 33 vakanın 24'ünde 256 girişin TAMAMI normatif double
  tabloya BİREBİR, kalanlar ±1 — .5 bağı çift-temsil kırılımı); saturation
  colorchannelmixer AYNEN. Ölçülen zarf (taranan küme: her param {-1,-0.5,0.5,1} tekil +
  fixtür kombinasyonu + 12 karışık = 33 vaka × 256 giriş × 3 kanal; beyan bu kümeye sınırlı):
  eski↔füzyon ≤ **±3 LSB** (28/33 vakada gerçek fark; m4/m6 3'e dokunur), füzyon↔normatif
  ≤ ±1 ve eski zincirin ±3'üne karşı her vakada YAKIN-YA-DA-EŞİT. Golden politikası:
  `ExportSnapshots/color-adjust.txt` yeniden temellendi (tek değişen snapshot); YENİ sınır
  golden'ı `ExportM5GoldenTests.ColorAdjustFusion_StaysWithinTheMeasuredEnvelope_AndConverges`
  `ToTheNormativeTable` (eski zincir testin içinde tarihsel referans; uyuşmazlık noktalarına
  bilerek düşer; yakınsama kırmızıysa eşik gevşetilMEZ); `ColorAdjust_MatchesTheNormative…`
  + `…IsCloserToTheNormativeMathThanTheEqMapping` YEŞİL (yalnız yapısal literal fused biçime
  güncellendi, tolerans değişmedi); GateInventory token envanteri yeniden ölçüldü (`exposure=`
  düştü). CANLI önce/sonra (compReal-1080p, aynı gün bitişik pencere, worker-run-g31 ↔
  worker-run-g3b; API g31 sabit): **41,6/41,1 → 36,1/35,5/34,8 s (p50 41,4 → 35,5; −5,9 s;
  1,45x → 1,69x)**. NEGATİF KONTROL: ofset aşamasının clip'i geçici düşürüldü → **2 kırmızı**
  tam imzayla (füzyon golden'ı "'fixtur': füzyon tablosu normatif referanstan ±1 LSB'den
  fazla saptı (2)" + `Compile_MatchesSnapshot(color-adjust)`) → geri al md5 birebir
  (`91c7b2ce…4b269`) + touch+rebuild → yeşil. Kapılar: build -warnaserror 0/0 · backend TAM
  **1592/1592 skip 0** (MinIO+ffmpeg) · editör 1342 + şema 222 · tsc -b + e2e tsc temiz.
  Doküman: rendering-semantics §4.1 YAZILIŞ GÜNCELLEMESİ kutusu + örnek zincir + exposure
  satırı + tablo terim notu.
- **Bacak 2 — (c) tuval-atlama ✅ (2026-09-01).** MEKANİZMA ÖLÇÜMLE ÇİVİLENDİ (karar şartı):
  rig'de iki aday karşılaştırıldı — eski −3,1 s sınıfını üreten şey, alt run tam-örtücüyken
  TABAN TUVAL + İLK OVERLAY'İN atlanmasıdır (g-real-nocanvas: p50 −2,1 s ve MP4 **SHA256
  birebir**); örtüsüz-pencere hızlı-yol dilimlemesi compReal'de yalnız [0,5)+[55,60) pencereyi
  kapsar (kazanç tavanı ~1/6) ve topoloji/sözleşme maliyeti yüksek → SEÇİLMEDİ. Uygulama:
  §2.6 örtücü yüklemi (`IsOpaqueFullCanvasRun` — CoversCanvas yerleşimi + worker'ın TAZE
  yerel-dosya probe olguları: kaynak aspect'i == tuval (tamsayı çapraz çarpım), SAR=1, alfasız
  pix_fmt izin listesi `AlphalessPixelFormats` + opaklık ≥1 + animasyonsuz; run-içi geçiş
  İZİNLİ) + `skipBase` (en alt run [0,toplam)'ı tek run kaplıyorsa taban tuval + overlay'i
  üretilmez; kompozisyon topolojisi/RGB rejimi/ses AYNEN, hızlı yola girilmez). Olgu
  altyapısı: MediaProbe/parser'a `pix_fmt` + `sample_aspect_ratio` (bilinmiyor=0/0; "0:1"
  kare-piksel kanıtı DEĞİL), ExportAssetSource'a PixelFormat/SarNum/SarDen, ExportJob taze
  probe'dan taşır — Bacak 3'ün altyapısı buraya ÇEKİLDİ çünkü (c)'nin bayt-güvenliği aynı
  olgulara muhtaç (alfalı/letterbox'lı taban tuval olmadan yanlış piksel üretirdi; doc-only
  varyant bilinçli RED). Golden politikası: HİÇBİR piksel golden'ı değişmedi (bayt-aynılık —
  eski↔yeni maxAbsDiff 0 / PSNR ∞; GoldenFrameTests geometri satırları dokunulmadı); yeni
  kalıcı muhafızlar: `ExportRenderGoldenTests.BaseCanvasSkip_IsByteIdentical_OnTheRealistic`
  `Topology` (çift-varyant CANLI render — aynı belge olgulu/olgusuz derlenir, tek fark 2
  satır, MP4'ler bayt-aynı; geçişli örtücü topolojisiyle) + `ExportCoverOptimizationTests`
  (yüklem envanteri: olgu-null/1919/yuva420p/bilinmeyen-pixfmt/SAR 2:1/SAR bilinmiyor/
  opacity 0.999/animasyonlu/açıkta-başlayan/PiP taban → atlama ATEŞLENMEZ; tek-run belge
  hızlı yolda kalır) + `ExportSnapshots/canvas-skip-base.txt` (yeni fixtür; mevcut olgusuz
  fixtürlerin snapshot'ları DEĞİŞMEDİ) + MediaProbeParser pix_fmt/SAR testleri. CANLI
  önce/sonra (compReal-1080p, bitişik pencere, g3b↔g3c): **35,9/35,6 → 32,7/32,0/32,3 s
  (p50 35,75 → 32,3; −3,4 s; 1,68x → 1,86x)** ve iki worker'ın AYNI belge çıktısı
  **SHA256-birebir** (`832ba5f3…59b6`, 34 878 279 B) — "belgelenen tuval renk-kaybı"
  bu mekanizmada NE AÇILDI NE KAPANDI (bayt-aynı; kaybın kapanışı tek-katman hızlı yol
  rejiminin işidir, bu bacağın mekanizması o değil — dürüst beyan). NEGATİF KONTROL: atlama
  koşulu geçici TERSLENDİ → **27 kırmızı** (yüklem bataryası + canvas-skip-base dahil
  snapshot'lar + çift-varyant bayt testi) → geri al md5 birebir (`caa1ef4b…e5f4`) +
  touch+rebuild → 44/44 yeşil. Kapılar: build -warnaserror 0/0 · backend TAM **1610/1610
  skip 0** (MinIO+ffmpeg) · editör 1342 + şema 222 · tsc -b + e2e tsc temiz. Doküman:
  rendering-semantics YENİ §2.6 (taşıyıcı norm + olgu kaynağı + örtücü yüklemi + atlama),
  CompiledExport.SourceWidth daraltılmış doktrin metni, ExportCompiler baş yorumu.
- **Bacak 3 — (a) örtülen-katman budaması ✅ (2026-09-01, daraltılmış doktrinle).** Penceresi,
  ÜSTÜNDEKİ TEK bir §2.6 örtücüsünün penceresince frame defterinde KAPSANAN run'ın video
  zinciri + overlay'i üretilmez (`prunedRuns` — Compile kompozisyon dalı); yüklem baş mimar
  kararının listesiyle birebir: örtücünün her segmenti medya klibi + CoversCanvas + taze
  probe'dan SourceW·H==SourceH·W + SAR=1 + alfasız pix_fmt + Opacity≥1 + Animation.Any==false;
  tek-run kapsaması (pencere birleşimi YOK); örtücü üstte; olgu bilinmiyorsa budama YOK;
  girişler + SES zincirleri DOKUNULMAZ (örtülen görünmez ama DUYULUR); topoloji korunur
  (hızlı yola girilmez; budama sonrası tek run kalırsa taban tuval de kalır — overlay'siz düz
  çıkış 2026-08-24 bisect'inin bayt-farkı sınıfıdır, o rejime girilmez). KANIT: (1) çift-varyant
  CANLI bayt-aynılık ×3 — `CoveredLayerPruning_IsByteIdentical_OnTheCutawayTopology`
  (sesli örtülen PiP'li cutaway, gerçek ffmpeg, MP4 bayt-aynı) + `BaseCanvasSkip_IsByte…`
  + GERÇEK medya belgesi CANLIDA: compCovered'ın g3c (budamasız) ↔ g3d (budamalı) worker
  çıktıları **SHA256-birebir** (`2566a184…c524`, 59 406 143 B); (2) canlı yakalanan g3d
  grafiği: [2:v] alt-açı + [3:v] şekil + [4:v] metin zincirleri YOK, [2:a] alt-açının SESİ
  DURUYOR, tek overlay (cutaway), tuval atlanmış; (3) yüklem envanteri
  (`ExportCoverOptimizationTests`): olgu-null/1919/yuva420p/SAR≠1/opacity 0.999/keyframe'li
  örtücü/pencereden taşan örtülen/alttaki örtücü/pencere birleşimi (iki komşu örtücü) →
  budama ATEŞLENMEZ; giriş listesi olgulu↔olgusuz birebir; (4) yeni snapshot
  `covered-layer-pruned.txt` (sesli örtülenin video zinciri yok, sesi + girişi var). CANLI
  önce/sonra (compCovered-1080p, bitişik pencere, g3c↔g3d): **28,0/28,7 → 20,1/20,1/20,0 s
  (p50 28,35 → 20,1; −8,3 s; 2,12x → 2,99x)** — örtülü sınıfın israfı kapandı. compReal'de
  budanacak şey yok (beklenen 0 — fixtür dürüstlüğü bunun için). NEGATİF KONTROL: pencere
  KAPSAMASI geçici KESİŞMEYE çevrildi → **5 kırmızı** (çift-varyant bayt golden'ı TAM
  sözleşme mesajıyla: "örtülen-katman budaması ÇIKTIYI DEĞİŞTİRDİ — §2.6 bayt-aynılık
  sözleşmesi ihlal: budanmış 79933 B, budanmamış 79940 B" + pencere-taşan/birleşim/ses
  bataryası + covered-layer-pruned snapshot) → geri al md5 birebir (`26794bd4…e8b8`) +
  touch+rebuild → yeşil. Kapılar: build -warnaserror 0/0 · backend TAM **1626/1626 skip 0**
  (MinIO+ffmpeg) · editör 1342 + şema 222 · tsc -b + e2e tsc temiz. Doküman: §2.6 budama
  bölümü + KAPSAM BEYANI (animasyonlu/merkez-dışı örtücü, pencere birleşimi, eğri-ekstremum
  örtme kanıtı KAPSAM DIŞI); DECISIONS +3 satır (daraltılmış doktrin + bayt-aynılık normu /
  colorAdjust füzyonu / N-paralel ERTELENDİ — reddedilenler ve geri alma koşullarıyla).

## Özellik turu (2026-09-01 onayı: 5 timeline özelliği — plan onaylı, ExitPlanMode)

> Plan: `C:\Users\tayfun\.claude\plans\capcut-ve-canva-gibi-hashed-penguin.md` (keşif 3 Explore +
> tasarım 2 Plan ajanı; kullanıcı kararları: J sessiz geri tarama, link=taşı+sil+böl, grup belgeye
> kalıcı, I/O ERTELENDİ). Commit öneki: `ozellik-<dilim>`. Her dilim: dört kapı + negatif kontrol
> + gerçek-girdi e2e.

| Dilim | İçerik | Durum | Not |
|---|---|---|---|
| 0 | Şema: linkId/groupId + invariant pass + codegen zinciri | ✅ YAPILDI (2026-09-01) | `linkId` (MediaClip) + `groupId` (klip tabanı) opsiyonel uuid; YENİ doküman-geçişli pass `checkLinkAndGroupInvariants` (kural 10-12: link TAM 2 klip = bir video + bir audio; grup ≥2 üye; link eşleri özdeş groupId; K4 savunması kind↔track-tipi — ekleme ÖNCESİ tarama: paket/editör/e2e-seed dokümanlarının hiçbiri kırılmadı, tam suite de doğruladı). Codegen: timeline.schema.json (opsiyonel uuid ref'leri) + TimelineContracts.g.cs (MediaClip'e nullable LinkId, tüm klip sınıflarına nullable GroupId; C# tarafına başka dokunuş yok). +13 paket testi (222→235; K1-K4 pozitif/negatif + alansız eski doküman DEĞİŞMEDEN geçerli pini). Negatif kontrol: link toplama söküldü → 4 yeni test tam imzayla kırmızı → md5-birebir geri (`0d5b20a6…55eff`). Kapılar: backend **1626/1626 skip 0** · editör 1342 · şema 235 · -warnaserror 0/0 · tsc -b + e2e tsc + prod build temiz |
| 1 | Track partisyonu (video üstte / ses altta) | ✅ YAPILDI (2026-09-01) | `insertTrackPositioned` (audio→push, video/overlay→ilk audio ÖNÜNE splice; çağıranlar: addTrack + addClipFromAsset newTrack; overlay klip-ekleme yolu BİLİNÇLİ unshift'te — "yeni katman en üstte" pinli kararı, indeks 0 partisyonu zaten sağlar, plan satırından bilinçli sapma yorumla kayıtlı); `trackMoveBlockReason`'a takas-sonrası partisyon kapısı `'audio tracks stay below video tracks'` (kilit/kenar kuralları ÖNCE konuşur; düzeltici yön serbest — karışık legacy belge fixture'la testli); feedback çevirisi (feedbackCoverage yeşil); ayraç: drawTracks'te TRACK_GAP içine 1px çizgi + TimelinePanel ilk ses başlığına border-top (border-box — yükseklik/geometri değişmedi). +8 birim test (timelineOps 57→65); e2e track-manage'e gerçek-fare senaryo: +A altta doğar → ses başlığı sağ-tık 'Yukarı taşı' gri + data-block-reason + Türkçe title → +V sesin ÜSTÜNE doğar. Negatif kontrol: partisyon düz push'a → 5 test tam imzayla kırmızı → md5-birebir geri (`066fadda…f53b`). Kapılar: backend 1626/1626 skip 0 · editör 1350 · şema 235 · -warnaserror 0/0 · tsc + e2e tsc + prod build · Playwright TAM suite kendi yayınıyla (api/worker-run-oz1, tazelik: modül yolu + UTF-8 LinkId/GroupId iğnesi, Vite taze): **165/165, 0 skip (10,5 dk)** — yeni partisyon senaryosu dahil (164→165), track-manage/context-menu/timeline-gates/library komşuları aynen yeşil |
| 2 | linkId çekirdeği: link/unlink + sil/böl/taşı kapanışı | ✅ YAPILDI (2026-09-01) | SAF kapanış `expandSelectionForOp(d, ids, 'move'\|'link')` (anchor önde, genişletme APPEND; 'move' groupId'yi ŞİMDİDEN işler — K3 sayesinde tek geçiş, birim testli); `linkClips`/`unlinkClips` + blockReason'lar (çift şekli / zaten bağlı / farklı gruplar / kilit; tek gruplu yanda groupId eşe de yazılır); kapanış OP İÇİNDE: deleteClips (eş kilitliyse TÜM silme RED `'linked clip is on a locked track'`; küçülen grup temizliği aynı mutate) + deleteTrack (eş bağı + grup temizliği) + splitAtPlayhead (çift taraf bölünürse sağ yarılara TAZE ORTAK linkId; `applySplitToDraft` ikinci klipten linkId'yi HER ZAMAN siler, groupId kalır) + moveClips (girişte 'move'); planMoveClips BÖLÜM-KAPSAMLI trackDelta (audio=1/diğer=0; anchor bölümü şerit değiştirir, öbürü yatay — :1109 tip kapısı KALIR); `remintLinkAndGroupIds` duplicate+paste'te (≥2 görülene taze ortak, tek görülen SİLİNİR; cloneClip dokunulmadı); detachAudio çifti BAĞLI doğurur (+ zaten-bağlı videoda detach RED — taze bağ eski eşi dangling bırakırdı, kod `'clip is already linked'` yeniden kullanıldı); trim'de kapanış YOK (negatif pin); TimelinePanel sürükleme ids kapanışlı + ghost şeridi plan.moves.toTrackIndex'ten; menü 'Bağla'/'Bağlantıyı kaldır' (gated sözleşme) + 5 feedback çevirisi (feedbackCoverage yeşil); drawTracks zincir rozeti (yükseklik değişmedi); appBridge ClipState linkId?/groupId?. +24 timelineOps + +5 guardPaths ayrışmazlık birimi (editör 1350→1379); e2e YENİ `link-clips.spec.ts` (gerçek fare: detach→linkId eşit→sürükle İKİSİ→Delete İKİSİ→TEK Ctrl+Z→unlink→yalnız biri→Shift+tık Bağla→gri-öğe data-block-reason+title) + detach-audio-silent'a linkId satırı. NEGATİF ×2 (md5-birebir `d40b1037…68a4`): (1) delete kapanışı söküldü → birim `timelineOps.test.ts:1281` kırmızı + e2e 'Delete bağlı çifti (video+ses) birlikte silmeliydi.' kırmızı; (2) bölüm-deltası söküldü → taşıma testi `{ok:false, reason:'track type mismatch'}` imzasıyla kırmızı. Kapılar: backend 1626/1626 skip 0 · editör 1379 · şema 235 · -warnaserror 0/0 · tsc + e2e tsc + prod build · Playwright TAM suite **167/167, 0 skip** (43 spec; api/worker-run-oz1 HEAD — backend değişmedi, süreç sağlığı + :5000 health doğrulandı) |
| 3 | Otomatik AV ayrımlı ekleme | ✅ YAPILDI (2026-09-01) | FRONTEND-only. SAF planlayıcı `planAddClipFromAsset` EXPORT: commit + TimelinePanel insertTargetFor (ghost geçerliliği + ÇOKLU insert ghost'u — AV asset'te video satırı + ses satırı) + blok gerekçeleri AYNI planı okur ("ghost geçerli dedi, drop reddetti" imkânsız — pasteBlockReason emsali). Karar tablosu: image/audio TEK; video+hasAudio=true ÇİFT (video `audio:null` + detachAudio formülü ses ikizi + ORTAK taze linkId; TEK mutate = TEK undo; İKİSİ seçili); video+hasAudio=false TEK ve `buildClipFromAsset` `audio:null` yazar (detach 'clip has no embedded audio' ile grilenir); hasAudio bilinmeyen TEK gömülü sesle (ikiz = ölçülmüş 422 tuzağı). Ses yerleşimi: ilk kilitsiz+aralıkta boş audio track; yoksa YENİ track (partisyon: EN ALTA) + notice 'audio placed on a new track' (drop işleyicisi reportOp'tan geçer — TRANSITION_DROPPED balon yolu); KISMİ BAŞARI YASAK: yeni track tavanı aşarsa (frontend `MAX_TRACKS=50` sunucu aynası; toplam yeni-track sayılır, 49→51 köşesi kapalı) TÜMÜ RED 'track limit reached'. +2 feedback çevirisi (feedbackCoverage yeşil). +14 birim (timelineOps 89→103; editör 1379→1393: karar tablosu 5 satır, tek undo (track dahil), yerleşim politikası 4, tavan 3, plan↔commit ayrışmazlığı 6 senaryo) + YENİ gerçek-fare `e2e/auto-av-add.spec.ts` (2 test: sürükle-bırak→2 linkli klip + ses track'i EN ALTTA DOĞAR + ikisi seçili + notice balonu; TEK Ctrl+Z ikisi+track geri; tekrar ekle→sürükle İKİSİ kayar; sessiz video (ffmpeg -an) + görsel negatifleri TEK klip). MEVCUT VARSAYIM TARAMASI (gerçek-medya spec'leri artık çift görür): media-upload, library-dnd (2 test), detach-audio-silent (sesli senaryo = oto-ayrılmış çift; sessiz gerekçe 'clip has no embedded audio'), export-flow, export-progress-hub (2→4), progress-fallback, frame-grid, guard-paths c2 (bölme 4 klip + video süzgeci), library-manage (kullanım '1 projede 2 klipte') güncellendi; seed'li spec'ler (hasAudio bilinmeyen) DEĞİŞMEDİ — politika bilerek böyle. NEGATİF ×2 (md5-birebir `44090a57…26a2`): (1) çift üretim söküldü → 8 birim tam imza + e2e 'Yeni ses track'i açıldığı halde bildirim balonu görünmedi.' KIRMIZI; (2) kısmi-başarı muhafızı söküldü → 2 'tümü RED' birim testi `{ok:true}≠{ok:false,'track limit reached'}` imzasıyla KIRMIZI. Kapılar: backend **1626/1626 skip 0** · editör 1393 · şema 235 · -warnaserror 0/0 · tsc + e2e tsc + prod build · Playwright TAM suite **169/169, 0 skip** (44 spec; api/worker-run-oz1 HEAD — backend değişmedi, süreç sağlığı + :5000 health + modül yolu doğrulandı) |
| 4 | Klip grupları (Ctrl+G) | ✅ YAPILDI (2026-09-01) | FRONTEND-only; dilim 0-3 hazırlığının (şema+invariant, 'move' kapanışı, remint, küçülen-grup temizliği, appBridge) ÜSTÜNE op+menü+kısayol+görsel. `groupClips`: önce LINK kapanışı (linkli üyenin eşi OTOMATİK dahil — K3 böyle KURULUR; tek linkli klip bile gruplanabilir, eş minimumu tamamlar), kapanış sonrası <2 → 'need at least two clips to group', kilitli üye → 'track is locked'; kapanmış kümeye TAZE tek groupId (BİRLEŞTİRME: eski üyelikler ezilir, eski gruptan tek kalan üyenin groupId'si AYNI mutate'te temizlenir — `cleanupShrunkenGroups`, K2/kural 11), etiket 'Klipler gruplandı'. `ungroupClips`: dokunulan HER grup TÜMDEN dağıtılır (üye çıkarma YOK — tek üyeli ara durum hiç doğmaz; kilitli üye grup NERESİNDE olursa olsun ret), linkId'ye DOKUNMAZ, etiket 'Grup dağıtıldı'. +2 feedback çevirisi (feedbackCoverage); menü 'Grupla' (Ctrl+G) + 'Grubu dağıt' (Ctrl+Shift+G) Bağla bloğunda gated sözleşmeyle; menuActions exhaustive 2 dal; dispatcher Ctrl+G / Ctrl+Shift+G (docMutationAllowed kapısı; çakışan ctrl tuşu yok) + shortcutsHelp satırı; drawTracks gruplu klipte 2px mor ÜST ŞERİT (satır yüksekliği/geometri değişmedi; zincir rozeti y+4'te başlar — çakışmaz). TAŞIMA kod değişikliği GEREKMEDİ (dilim-2 'move' kapanışı groupId'yi zaten işliyor) — grup-odaklı pinlerle sabitlendi. +21 birim (timelineOps 103→117: pozitif/K3/birleştirme/K2/blok dalları/ungroup/karışık-AV grup taşıma (bölüm-deltası grup üzerinden)/hep-ya-hiç/silme pinleri/remint pinleri; guardPaths 62→67: (e) ayrışmazlık çiftleri ×5; dispatcher 24→26: ctrl+g / ctrl+shift+g + editable-pasif + oturum kapısı; editör 1393→1414). e2e YENİ `group-clips.spec.ts` (2 test, GERÇEK fare/klavye): 3. klip gerçek jestle (scrub + sağ tık böl) → marquee 3 klip → sağ tık Grupla → ortak groupId; seçim düz tıkla TEKE indirilip sürükle → ÜÇÜ birden (kapanış kanıtı — çoklu-seçim maskesi bilinçli kaldırıldı, NC1 bunu ölçtü); tek üye Delete → yalnız o, kalan İKİLİ grup yaşar → tek Ctrl+Z; Ctrl+Shift+G → dağıldı → sürüklemede yalnız biri (negatif); Ctrl+A+Ctrl+G klavye yolu + etiket; gate testi 'Grupla' gri + data-block-reason + Türkçe title, 'Grubu dağıt' gri. NEGATİF ×2 (md5-birebir `af2fea03…e5eb`): (1) expandSelectionForOp groupId genişletmesi söküldü → 4 birim kırmızı (yeni imza `timelineOps.test.ts:1698` 'Grup üyesi ses de kaydı.: expected +0 to be 2000000' + hep-ya-hiç `{ok:true}≠{ok:false,…}` + dilim-2 kapanış pinleri) + e2e `group-clips.spec.ts:94` 'Sürüklenen üye kaymalı.' expected >0 received 0 KIRMIZI; (2) groupClips eski-grup temizliği söküldü → MERGE birimi dev doc KAPISINDAN kırmızı: `Timeline invariant violation after "mutate(group)": … group invariant violated: groupId … has 1 member(s), a group is at least 2 clips` (K2 ihlalini kapı da yakalıyor — plan beklentisi doğrulandı). Kapılar: backend **1626/1626 skip 0** · editör 1414 · şema 235 · -warnaserror 0/0 · tsc + e2e tsc + prod build · Playwright TAM suite **171/171, 0 skip** (45 spec; api/worker-run-oz1 HEAD — backend değişmedi, süreç sağlığı + :5000 health + modül yolu doğrulandı, kaçak ffmpeg 0) |
| 5a | J shuttle: transport store + tek-hız sessiz geri tarama | ✅ YAPILDI (2026-09-01) | FRONTEND-only; `engineV1.ts` DOKUNULMADI (plan kararı: motor API'sine yön REDDEDİLDİ). YENİ `shortcuts/shuttle.ts`: `useTransportStore` ({forwardRate, shuttleRate}) — dispatcher'ın `forwardRate` modül-let'i store'a TAŞINDI (K/Space/togglePlayback sıfırlama disiplini iki alana da); 33 ms metronom + İÇ FLOAT akümülatör `virtualUs` (store'un kare-yapışık değerinden geri hesap 30fps'te yuvarlanma-stall'ı üretirdi) + Date.now delta; her yazım kare-ızgara-yapışık `setPlayheadUs('user')` + yazım SONRASI kendi seq kaydı (zustand senkron) → mevcut scrub yolu kareyi getirir (PlayerPanel userSeekSeq aboneliği → engine.seek precise:false; `SCRUB_THROTTLE_MS` DOKUNULMADI — fare scrub'ıyla ortak). Sessizlik YAPISAL: motor paused, hiçbir element play() almaz, ses koduna dokunulmadı. İptal TİK İÇİNDE: isPlaying → dur; dış user seek (seq farkı — timeline scrub/ok/Home, ayrı kanca yok) → dur, kullanıcı hedefi AYNEN kalır; virtualUs<=0 → TAM 0 + dur (BOF kelepçesi). Geçiş matrisi: J = forwardRate 1 + pause + (!e.repeat) startOrBumpShuttle, repeat yutulur (eski 'v1: no reverse' pause+stepSeconds(-1) dalı SİLİNDİ); K = stop + pause; L = stop + İLERİ oynat (shuttle hızı DEVRALINMAZ — isPlaying false olduğundan kademe 1x'ten); Space = stop + OYNATMAYA GEÇMEZ (NLE uzlaşımı: shuttle'da Space=dur). PlayerPanel rozeti `transport-shuttle-note` role=status 'Geri tarama 1x — ses kapalı' + title'da 'kare-adımlamalı yaklaşık geri tarama; gerçek geri oynatma v2' dürüstlük notu (`previewRate$` sarı rozeti KULLANILMADI — farklı dürüstlük mesajı, plan reddi). +9 birim (shuttle.test.ts 4: fake timer [interval/timeout/Date] hız dürüstlüğü 1000ms→~1sn ±1 kare + HER yazım ızgarada + metronom temposu; BOF tam 0 + timer GERÇEKTEN ölü; dış-seek iptali; isPlaying iptali · dispatcher.test.ts +5 matris: J pause + engine.seek HİÇ yok + store geri akar + isPlaying false; K sabitler; L 1x'ten; Space'te engine.play ÇAĞRILMAZ; editable'da j pasif; editör 1414→1423). e2e YENİ `jkl-shuttle.spec.ts` (GERÇEK klavye, 2 test): scrubTo(12s)→J→İKİ örnekte kesin küçülme + isPlaying FALSE KALIR (sessiz tarama kanıtı) + rozet metniyle; K→400 ms sabit + rozet yok; L→isPlaying true + artar; BOF: scrubTo(1.5s)→J→poll TAM 0 + rozet yok + 300 ms sabit. HIZ ÖLÇÜMÜ e2e'de YAPILMADI (kırılgan — birim testte). NEGATİF KONTROL (md5-birebir `99921c57…383e`): BOF kelepçesi söküldü → birim `expected 1 to be null` (BOF testi) KIRMIZI + e2e `BOF'ta shuttle kendiliğinden durmalı (rozet kalkar): unexpected value "visible"` KIRMIZI → geri al → yeşil. Kapılar: editör **1423** · şema 235 · tsc + e2e tsc + prod build temiz · Playwright jkl-shuttle+player+frame-grid+timeline-gates+a11y-smoke **28/28** (backend DOKUNULMADI — api/worker-run-oz1 sağlık + yüklü modül yolu doğrulandı; TAM suite 5b kapanışında) |
| 5b | J shuttle: kademe merdiveni + rozet + yardım/karar kayıtları | ✅ YAPILDI (2026-09-01) | TUR KAPANIŞI (5 özellik / 7 dilim TAMAM). Merdiven: J tekrar basışları shuttleRate'i 1→2→4→8 katlar, 5. basış 8'de KALIR (`SHUTTLE_MAX_RATE`); katlama bir SONRAKİ tikin deltasından işler (geçmiş dilim yeniden fiyatlanmaz). J VE L'ye `e.repeat` muhafızı: basılı tutmak OS auto-repeat'iyle kademeyi fırlatmaz, olay yine yutulur (true) — L'de küçük savunulabilir davranış değişikliği, DECISIONS satırında anıldı. Rozet DİNAMİK: 'Geri tarama {n}x — ses kapalı'; AYNI bileşen forwardRate>1 + oynarken 'İleri {n}x' der (iki durum aynı anda yaşayamaz — shuttle yalnız motor paused'ken; öncelik geri taramanın). shortcutsHelp J/K/L satırı: 'Geri tara (J tekrar: 2x…8x, sessiz) / Duraklat / Oynat (L tekrar: 2x…8x)'. Ölü yüzey temizliği: kullanılmayan `isShuttleActive` export'u kaldırıldı. +6 birim (shuttle: merdiven matrisi 1→2→4→8→8 + 2x kademede 1000 ms'de ~2 sn ±1 kare hız dürüstlüğü; dispatcher: J merdiveni dispatch yolundan, J(repeat) bumplamaz ama true döner, L(repeat) aynı, L 4x'teyken J → shuttle 1x'ten + forwardRate 1'e; editör 1423→**1429**) + e2e ikinci-J '2x' rozet iddiası (`jkl-shuttle.spec.ts` test 1'e). KAYITLAR: DECISIONS satırı (motor DEĞİL dispatcher-shuttle; reddedilenler: motor API'sine yön/negatif-rate — `<video>` negatif playbackRate oynatamaz + re-seek fırtınası + saat/syncElements/ses-zarfı üç kritik bölge; SCRUB_THROTTLE_MS düşürmek — fare scrub'ıyla ortak, decoder'ı boğar; previewRate$ rozetini yeniden kullanmak — farklı dürüstlük mesajı. Geri alma: v2 WebCodecs gerçek reverse getirince J motora bağlanır); tasarım 01 §3.4 satırı + dipnotu (J v1'de kademeli SESSİZ kare-adımlamalı geri tarama — kullanıcı kararı 2026-09-01; gerçek reverse v2); poc-bilinen-sinirlar YENİ §2.8 (ses yok/yaklaşık tarama/v2 işi). 2. NEGATİF KONTROL (md5-birebir `27249d47…9ada`): bump ×2 sabit 1'e söküldü → 4 birim KIRMIZI (merdiven ×2 `expected 1 to be 2` + hız dürüstlüğü `2x hızda 1000 ms sonra ~10 sn beklenirdi, ölçülen 11000000 µs` + dispatcher merdiven/repeat çifti) + e2e `İkinci J kademeyi 2x'e katlamalı: unexpected value "Geri tarama 1x — ses kapalı"` KIRMIZI → geri al → yeşil. KAPILAR (TAM, kendi koşumum): backend build -warnaserror 0/0 · `MINIO_AVAILABLE=1 dotnet test` **1626/1626 skip 0** (2 dk 34 sn) · editör **1429** · şema 235 · tsc -b + e2e tsc + prod build temiz · Playwright **TAM SUITE 173/173, 0 skip (46 spec, 11,1 dk)** — jkl-shuttle 2 testi dahil (171→173); ortam TEK SAHİP: api/worker-run-oz1 (backend değişmedi; sağlık + yüklü modül yolu doğrulandı), kaçak ffmpeg 0 |
| D | Tur kapanış denetimi (baş mimar) | ✅ ONAY (2026-09-01) | ONAY + 3 bulgu (2 orta + 1 düşük), üçü de `ozellik-duzeltme` commit'iyle KAPANDI: (1) deleteTrack kilitli link-eşin bağını SESSİZCE siliyordu (deleteClips aynı durumda `'linked clip is on a locked track'` ile reddediyordu — ölçüldü) → artık TÜMDEN RED aynı mesajla (`trackDeleteBlockReason`'a kural; menü gated sözleşmesiyle otomatik gri; kilitsiz-eş bağ temizliği AYNEN; +1 birim + guardPaths (f) ayrışmazlık çifti; negatif kontrol: ret söküldü → 2 test `expected null to be 'linked clip is on a locked track'` KIRMIZI → md5-birebir geri); (2) Ctrl+X yarım-çift: pano yalnız seçileni alıyor, silme çifti düşürüyordu → ses içeriği kes-yapıştır akışında kayboluyordu (ölçüldü) → cutClips kopyayı da LINK-kapanışlı kümeden yapar: pano 2 klip, yapıştırmada remint taze ortak linkId; Ctrl+C BİLİNÇLİ asimetrik — kopya belgeden eksiltmez (+3 birim [yarım/tam kes + kopya-asimetri pini] + e2e gerçek-klavye Ctrl+X→Ctrl+V `link-clips.spec.ts`; negatif kontrol: kes-kapanışı söküldü → birim 'Pano çifti BİRLİKTE taşımalı' imzasıyla KIRMIZI → md5-birebir geri); (3) CLAUDE.md yetenek haritası 5 timeline özelliğiyle senkronlandı (davranış sıfır). DECISIONS 2 satır (reddedilenlerle). Denetçinin bağımsız ölçümleri: PUT/GET birebir, export bayt-birebir, editör perf ~14 ms/2000 klip |
