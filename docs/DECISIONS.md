# DECISIONS — karar günlüğü
Son güncelleme: 2026-09-03. Reddedilen alternatifler kritiktir: yeni session aynı yola sapmasın.
Tam gerekçe ve ölçümler için `docs/backlog.md` (tur kayıtları) ve `docs/rendering-semantics.md`.

| Karar | Tarih | Gerekçe | Reddedilen alternatif | Geri alma koşulu |
|---|---|---|---|---|
| Zaman = tamsayı mikrosaniye; float saniye YASAK | 2026-08-06 | İki dilde birebir aynı aritmetik | Float saniye | Yok (temel sözleşme) |
| Geçiş = bitişiklik + D/2 kaynak payı (metadata) | 2026-08-06 | Overlap modeli çakışma değişmezini bozuyordu | Klip overlap'ı | Yok |
| Frame-grid kuralı KENARLARDA (start ve end), sürede değil | 2026-08-07 | Grid toplama altında kapalı değil (NTSC); süre kuralı %36 yanlış ret üretti | Süre-bazlı grid | Yok |
| Kural sınıfı: senkron 422 kapıları + muhafız defterleri | 2026-08-12..21 | "Kural yalnız Compile'da" sınıfı 6 kez kuyruk-sonrası ölüm üretti | Kuralı worker'da bırakmak | Yok — W4 disiplini |
| Geçişli tek-kutu: pad hedefi `Box & ~1`, parite kısıtı KALDIRILDI | 2026-08-12 | Ölçüm: kısıt kendisi 1 px kayma üretiyordu; %74 ölçek yasaklıydı | Editörde ölçek nicelemesi (B planı) — kullanıcıyı anlaşılmaz ızgaraya hapsederdi | ffmpeg scale çift-çıktı sözleşmesi değişirse |
| Overlay koordinatı ifadede açık `floor(P)` | 2026-08-21 | ffmpeg `(int)` SIFIRA doğru kırpar; pad'li/pad'siz yol 1 px ayrışıyordu (dikeyde %43 ulaşılabilir) | trunc davranışına doküman uydurmak | Yok |
| Bellek okuyucu = COMMIT boşluğu (`ullAvailPageFile`) | 2026-08-24 | Fiziksel-boş okuma canlıda 2160p'yi YANLIŞ reddetti (Windows çalışma kümelerini kırpar) | `ullAvailPhys` | Yok — canlı yanlış-ret kanıtı arşivde |
| 503 yalnız KURULUM arızası; belge kusuru daima 422 | 2026-08-13 | Bilinmeyen fontId 503 alıp "yeniden dene" diyordu — asla çalışmazdı | Geniş catch ile hepsine 503 | Yok |
| Export profili farklı en-boy oranını REDDEDER (tipli 422) | 2026-08-21 | Letterbox yarım piksel + önizlemesiz kare doğuruyordu (ölçüldü) | Letterbox | Önizleme letterbox'ı da gösterirse |
| LUT blend yerli yazılış + ±1 LSB zarfı NORMATİF | 2026-08-25 | 0,87x→1,60x; fark yalnız dyadik-olmayan yoğunlukta ±1 LSB (65k çift tam tarama) | Revert (kazanç kaybı); "bayt-aynı" iddiasını sürdürmek (ölçümle yanlıştı) | ffmpeg blend yuvarlaması değişirse (golden sınır testi yakalar) |
| Bezier: şema kelepçesi REDDEDİLDİ; kapılar eğri EKSTREMUMUNU okur | 2026-08-25 | Kelepçe overshoot/back/elastic preset sınıfının önünü keserdi; kapalı-form ekstremum ~2400× hızlı ve kesin | y1/y2'yi [0,1]'e kelepçelemek | Yok |
| media-urls: toplu backfill YOK → TEMBEL backfill | 2026-08-24..25 | Toplu migration satır başına storage GET'li riskliydi; yedek yol zaten doğru servis eder | Toplu backfill migration'ı | Eski asset sayısı sorun olursa (bugün kendiliğinden eriyor) |
| Kota türevleri sayar (`Asset.DerivedBytes`) | 2026-08-21 | Türevler ~%7,7 bedava alandı | Yalnız orijinali saymak | Yok |
| SkiaSharp 3.116 PIN | (csproj yorumu) | Tüm golden PNG'ler bu sürüme kalibre | Serbest yükseltme | Planlı pencere: yükselt + golden'ları yeniden doğrula |
| WorkerCount=1 (export kuyruğu) | tasarım | ffmpeg zaten tüm çekirdekleri kullanır | Süreç içi paralel export | Çok-worker işletim provası yapılırsa (testi var) |
| Lint YOK | 2026-08-22 | KULLANICI KARARI (AskUserQuestion) — "bitti" = build -warnaserror + testler + tsc | ESLint kurulumu | Kullanıcı isterse |
| Push SONRA | 2026-08-22 | KULLANICI KARARI — origin bilinçli geride | Anında push | Kullanıcı "push" derse |
| R2 dağıtımı EN SONA | 2026-08-25 | KULLANICI KARARI — önce tüm borçlar | Önce R2 | Kullanıcı anahtarları verip başlatınca |
| SignalR/Redis: GETİRİLECEK (kullanıcı kararı) — UYGULANDI | 2026-08-31 | Kullanıcı AskUserQuestion ile "SignalR'ı getir" seçti; tasarım 03 spec'i AYNI GÜN uygulandı: `JobProgressHub` (`/hubs/progress`, abonelik REST'le aynı sahiplik kapısından), worker her progress DB yazımının yanında Redis `job-progress` publish'i (`JobProgressChannel` tek tanım — Contracts), API'de `RedisProgressForwarder` → grup yayını; JWT query-string YALNIZ hub yolunda + istek logu query'siz (redact). DB %5-adım yazımı ve polling AYNEN durur (yedek + sessizlik bekçisi `HUB_SILENCE_TIMEOUT_MS`); Redis erişilemezse API/Worker açılır ve polling taşır (Redis zorunlu DEĞİL) | İskeleyi sökmek; şimdilik dokunmamak. UYGULAMADA reddedilen: **SignalR Redis backplane paketi** — tek API instance'ında hub'lar arası senkron yok, düz pub/sub aboneliği yeter (paket eklemek ölü bağımlılık olurdu); **worker'a HubConnection istemcisi** — worker'ı API'nin ayaktalığına bağlardı, pub/sub bağlamaz | Backplane: API çok-instance'a çıkarsa `Microsoft.AspNetCore.SignalR.StackExchangeRedis` eklenir (forwarder yerine backplane grupları dağıtır). Kanalın kendisi: kullanıcı vazgeçerse |
| B6 user-feed: yayın kaynağı WORKER-publish (ownerId alanı) + abonelik PARAMETRESİZ | 2026-08-31 | Pasif sekme "listende yeni satır doğdu"yu ancak sahip-bazlı gruptan duyar (id-bazlı abonelik sekmenin bildiği satırları taşır — canlıda ölçüldü); worker zaten her progress yazımında publish ediyor, mesaja `ownerId` (Jobs.RequestedBy) eklemek + forwarder'da `user:{id}` hedefi EN KÜÇÜK değişiklik; complete→ilk publish gecikmesi n=3: 0,62-1,79 s (p50 0,74) — kullanıcı algısı için yeterli. Parametresiz `SubscribeUserFeed` kimliği JWT'den türetir: başkasının feed'i hub yüzeyinde İFADE BİLE EDİLEMEZ | API'ye upload-complete anında İKİNCİ publish yönü açmak (kazanç <2 sn; yeni publisher yüzeyi + API'ye Redis yazma bağımlılığı açardı); id-parametreli SubscribeUserFeed(userId)+sahiplik sorgusu (gereksiz DB turu + saldırı yüzeyi); feed'i sessizlik bekçisine/polling kapısına bağlamak (feed'in sessizliği normaldir — iş yokken mesaj beklenmez; kapatacağı bir bilinmeyen-satır yoklaması da yok) | Kullanıcı SignalR kanalından vazgeçerse kanalla birlikte söner |
| Silme senkronu (2a): API-kaynaklı olay SÜREÇ-İÇİ IHubContext yayını + ayrı küçük tel tipi (`AssetRemovedMessage`/`assetRemoved`) + istemcide liste CERRAHİSİ | 2026-08-31 | Silme worker işi doğurmaz → `progress` akışına hiç girmez; hub'ı API'nin KENDİSİ barındırır — soft-delete sonrası sahibinin `user:{id}` feed grubuna doğrudan gönderim EN KÜÇÜK yol (canlıda ölçüldü: satır 33 ms'de düştü, ilk çerçeve +21 ms). Yayın yalnız durum DEĞİŞTİREN silmede (etkilenen satır=1; idempotent 2. DELETE olaysız) ve best-effort (204 sonrası yayın hatası 500'e çevrilmez). İstemci satırı cache'lerden düşürür + YALNIZ kotayı invalidate eder (tek olay = tek GET; silme sonrası liste GET'i 0 — spec ölçümü) ve silinen id'yi noticed setine yazar (geç worker mesajı diriltemez; terminal invalidate'in listesi zaten sunucu-otoriter) | API'ye Redis publish yönü açıp forwarder'dan geçirmek (B6'da reddedilen API-publisher yüzeyi; olay hub'la AYNI süreçte doğuyor — tur sıfır kazanç, yeni bağımlılık); silmeyi `JobProgressMessage`'a sahte status'la bindirmek (silme iş değil — status sözlüğü kirlenir); istemcide liste invalidate (olay başına fazladan liste GET'i — üyelik bilgisi 204 dönen silmenin kendisinden zaten kesin) | API çok-instance'a çıkarsa süreç-içi gönderim yetmez — yayın backplane'e/forwarder'a taşınır (SignalR backplane satırının geri alma koşuluyla birlikte) |
| Revision retention (2b): tasarım tarifi "son 50 auto + 24 sa'ten eskilerde saatte 1'e inceltme"nin YORUMU — genç kayıt (≤24 sa) son-50 DIŞINDA da SİLİNMEZ; inceltme yalnız eşiği aşmış yaşta işler; Checkpoint VE PreRestore kapsam dışı | 2026-08-31 | Tasarım 03 §"Snapshot politikası" iki kuralı yan yana sayar ama kesişimi tanımlamaz. Uygulanan okuma: (1) proje başına en yeni 50 Auto her koşulda kalır; (2) 24 saatten genç Auto'lar sayı sınırına takılmaz (yoğun bir günün geçmişi aynı gün silinmez); (3) daha eskiler saat kovasına inceltilir (kovayı TUTULAN herhangi bir kayıt temsil eder → "tam 50'de dokunmaz, 51.'de en eskiyi süpürür" sınırı sağlanır). PreRestore tarife eklenmedi ama korunur: restore'un TEK kurtarma yoludur, versions UI'ı ona dayanır. Kural [Queue("transcode")] saatlik recurring işte (`revision-retention`), sabitler config'te (`RevisionRetention__*`, açılışta Validate + etkin-değer logu). Canlı ölçüm: dev DB'de 15 861 Auto birikmişti (29 MB); seed 60 eski auto + 1 checkpoint + 1 prerestore → tetiklenen canlı iş 10 eskiyi süpürdü, en yeni 50 + iki korumalı tür kaldı | Katı okuma "50'den sonrası yaşa bakılmadan silinir" (aynı günün geçmişini süpürür — versiyon geçmişi özelliğini fiilen bozar); kova temsilcisini yalnız adaylar arasından seçmek (51 kayıtlık sınır durumunda hiçbir şey silinmez — tasarımın "51.'de süpür" beklentisi bozulur); PreRestore'u da inceltmek (restore'un tek kurtarma yolunu siler); silme sorgusundan Kind şartını düşürmek (seçici regresyonunda Checkpoint DB'den gidebilirdi — çifte kilit) | Tasarım tarifinin kendisi değişirse (ör. günlük kova, farklı sayı) sabitler config'ten, kural bu satır güncellenerek |
| E2E hesap temizliği (2c): dev-yönlü ELLE koşulan betik (`scripts/cleanup-e2e.ps1`), globalTeardown'a BAĞLANMADI; ürüne kullanıcı-silme ucu AÇILMADI | 2026-08-31 | Her Playwright koşumu worker başına kalıcı e2e hesabı bırakır; birikim ölçüldü: 946 hesap / 15 435 proje / 15 290 revizyon / ~7 GiB + 10 376 MinIO objesi. Betik yalnız compose.dev.yml konteynerlerine karşı çalışır (bağlantı dizesi parametresi YOK — yanlış/prod hedef İFADE EDİLEMEZ; ek fail-fast: compose config etiketi + current_database kontrolü); aday deseni SABİT iki üreticinin birleşimi (`e2e-*`/`e2e-auth-*` @videoedit.test), demo ya da desen dışı aday görülünce exit 2 ile hiçbir şey silinmeden çıkar (SQL tarafında da aynı süzgeç — çifte kilit). Varsayılan ELLE koşum: teardown her koşumu docker-exec turlarıyla şişirir, kırık koşumların artığı hata ayıklamada delildir ve teardown paralel oturumların ortamıyla yarışabilir | Playwright globalTeardown'a bağlamak (suite süresi + yarış + delil kaybı); ürüne DELETE /api/users ucu açmak (prod yüzeyi büyütmek bu işin kapsamı değil; IDOR/kota matrisine yeni uç sokar); MinIO lifecycle'a bırakmak (dev MinIO'da kural yok ve DB satırlarını hiç kapsamaz) | Birikim yeniden sorun olursa cron/teardown'a bağlama kararı kullanıcıya sorulur; ürün-içi hesap silme ayrı bir özellik kararıdır |
| XML-doc: yapısal uyarılar hata; CS1591 susturuk; tests/ kapsam dışı | 2026-08-24 | 39 yapısal uyarı gerçek içerikle kapatıldı; 404 şablon özet değersiz; tests ~931 uyarı açardı | Tüm public üyelere özet yazmak | Özet zorunluluğu istenirse NoWarn satırı silinir |
| Kod yorumlarında satır numarası / doğrulanmamış sayı YASAK | 2026-08-22 | Bir turda 61 atıf bayatladı; üç sayı iddiası ölçümle yanlışlandı | Satır atıflı yorumlar | Yok |
| E2E kanıtı yalnız GERÇEK girdi (page.mouse/keyboard) | 2026-08-07 | Sentetik olaylar sahte yeşil üretti; kullanıcı "hiçbir şey çalışmıyor" dedi — kurucu ders | dispatchEvent/store çağrısıyla test | Yok (review-gate kural 3) |
| Örtülen-katman budaması + taban-tuval atlaması: doktrin DARALTILDI (geometri mutlak kaynak-bağımsız; taze worker-probe olgusu üyeliği yalnız sayılı kapılarda etkiler) + bayt-aynılık normu (§2.6) | 2026-09-01 | Baş mimar kararı (gelistirme-3 #3): probe'un üyelik etkisi zaten emsalliydi (HDR tonemap zinciri); budama kararını worker'ın ffmpeg'in decode edeceği YEREL dosyaya çektiği taze ffprobe verir; kanıt yükü çift-varyant framemd5/SHA256 BAYT-AYNILIĞI (canlı ölçüldü: compReal g3b↔g3c ve compCovered g3c↔g3d MP4'leri sha256-birebir); yüklem tamsayı aritmetiği (SourceW·H == SourceH·W çapraz çarpım + SAR=1 + alfasız pix_fmt izin listesi), bilinmeyen olguda budama YOK | Tam ret ("riskli" demek ölçümsüz temkin olurdu — ölçülmüş israf sınıfı açık kalırdı); GENİŞ yüklem (keyframe'li/pencere-birleşimli örtücüyü eğri ekstremumuyla kanıtlamak — KAPSAM DIŞI ilan edildi, ayrı ölçümlü ayrı karar); kararı API-side DB defterinden (ExportAssetFacts) vermek (bayat-probe riski); budama sonrası tek run kalınca hızlı yola yeniden girmek (hızlı yol yuv420p — çift-varyant bayt-aynılığı düşerdi); overlay'siz düz çıkışa taban atlaması (encoder'a farklı ara format — 2026-08-24 bisect'inin bayt-farkı sınıfı) | Çift-varyant golden bir kez bile fark ölçerse ilgili optimizasyon KAPATILIR (bayrakla değil koddan) |
| colorAdjust füzyonu: aşama 1-4 kanal-başına tek lutrgb bileşik ifadesi (DOUBLE + aşama-başına clip + tek nihai round) + ölçülen zarf normatif (§4.1 yazılış kutusu) | 2026-09-01 | Rig p50 41,4→34,4 s (eski `exposure` float filtresi + aşama-başına 8-bit kesme zincirin en pahalı halkasıydı); round-final tablo normatif double referansa 33 vakanın 24'ünde 256 girişte BİREBİR, kalanı ±1 LSB (eski zincir ±3'e açılıyordu) — normatif matematiğe ve GLSL'e YAKINSAMA; eski↔füzyon zarfı ≤±3 LSB golden'la çivili | Bayt-aynı taklit (ara 8-bit nicelemeyi ifadede taklit etmek) başlangıç hedefi olarak — normatiften uzaklaşırdı; `eq`'ya dönüş (4.1.1'de zaten reddedilmiş) | Zarf golden'ı ffmpeg sürümüyle kırılırsa (ColorAdjustFusion golden'ı yakalar) |
| N-paralel dilimleme ERTELENDİ (gelistirme-3 #3 (d) bacağı) | 2026-09-01 | Tek grafik zaten ~13,5/20 çekirdek; bileşim örnek başına ~2,1-2,5 GB ek RSS (2160p tepe 5 563 MB) → N dilim bellek kapısının modelini çarpanla bozar; dilimli kodlama rate-control sıfırlamasıyla tek-geçiş bitstream'iyle karşılaştırılamaz (tüm golden/karşılaştırma şasisi yeni sözleşme ister); WorkerCount=1 kararının gerekçesiyle de çelişir | Bu turda açmak | Bacaklar sonrası gerçekçi fixtür <2x VE kullanıcı hedefi sürdürürse kendi sözleşme turuyla (dilim sınırları geçiş+örtme pencereleri dışında; closed-GOP/keyint sabitleme; bellek kapısı çarpanı) |
| Export perf kabul ölçütü DARALTILDI ve KAPANDI: gerçekçi 1080p ≥1,8x (ölçülen 1,85x) + örtülü 2,99x + dikey 2,10x + 720p 1,94x | 2026-09-01 | KULLANICI KARARI (AskUserQuestion) — ölçülmüş durum kabul edildi; (d) N-paralel'in sözleşme maliyeti/kazanç oranı (üstteki satır) hedefteki ~2,4 s'lik farkı karşılamıyor | (d)'yi açıp ≥2x'i kovalamak; hedefi süresiz açık bekletmek | Kullanıcı hedefi yeniden yükseltirse (d) kendi sözleşme turuyla açılır |
| Track düzeni = FİZİKSEL PARTİSYON, op-politikası olarak (ozellik-1): `insertTrackPositioned` (audio→push, video/overlay→ilk audio ÖNÜNE; overlay klip-ekleme yolu unshift'te kalır — en üst katman kararı) + `trackMoveBlockReason` ihlal takasını bloklar, düzeltici yön serbest; otomatik normalize YOK | 2026-09-01 | Ses track'leri z-order-nötr (derleyici track.Type'ı okumaz) → partisyon görsel çıktıyı değiştirmez; op-politikası mevcut karışık belgeleri kilitlemez, kullanıcı taşımasıyla düzelir; ayraç 6px TRACK_GAP İÇİNE 1px çizgi + DOM border-top (border-box) — satır formülleri değişmedi, geometri 3 kopyası aynen | Görsel satır↔indeks dolaylaması (3 kopyalı geometriye yayılırdı); doküman invariantı (legacy belgeyi kilitler — MAX_LAYER_DIMENSION emsali); açılışta otomatik normalize (istenmeyen autosave/revizyon kirletmesi) | Yok (davranış sözleşmesi; kullanıcı karışık düzeni açıkça isterse yeniden tartışılır) |
| Link ve grup AYRI opsiyonel alanlar: `linkId` yalnız MediaClip'te, `groupId` klip tabanında; schemaVersion=1 KALIR (ozellik-0) | 2026-09-01 | AV bağı ile grup FARKLI sözleşmeler taşır (link: TAM 2 üye, bir video + bir audio, taşı+sil+böl birlikte; grup: ≥2 üye, her klip türü, yalnız birlikte taşıma) ve invariantlar ancak ayrı alanlarla doğrudan ifade edilir (invariants.ts kural 10-12; aynı pass'e kind↔track-tipi savunma kuralı da girdi — audio track'teki video klip export'ta ÇİZİLİRDİ, şimdi belge reddi); yalnız opsiyonel alan eklendiği için eski dokümanlar DEĞİŞMEDEN geçerli (testte pinli) | Tek `linkGroupId` + tür ayrımı alanı (op başına dallanma; "TAM 2, video+audio" ile "≥2, her tür" tek alanın invariantında ifade edilemez); `linkId`'yi klip tabanına koymak (AV semantiğini sulandırır — text/shape'in "AV eşi" anlamsız) | Yok (veri modeli sözleşmesi) |
| Bağ/grup kapanışı OP İÇİNDE (`expandSelectionForOp`, timelineOps) + taşımada BÖLÜM-KAPSAMLI trackDelta (ozellik-2) | 2026-09-01 | Kapanış op girişinde koşar (deleteClips/splitAtPlayhead/moveClips): panel yalnız ghost için AYNI yardımcıyı kullanır — hangi çağıran olursa olsun (Delete tuşu, menü, kısayol, gelecekteki bir yol) eş işleme girer; eş kilitliyse silme TÜMDEN reddedilir (yarım silme = dangling linkId, sessiz düzeltme sınıfı). trackDelta bölüm-kapsamlı (`sectionOf = audio?1:0`): yalnız anchor'ın bölümü şerit değiştirir, diğer bölüm kendi şeridinde yatay kayar — aksi halde bağlı çift dikey taşınamazdı (ses şeridi video track'ine itilir, tip kapısı tüm taşımayı keserdi); :1109 tip kapısı bölüm İÇİNDE aynen yük taşır (negatif kontrollü) | Panel-başına genişletme ('unutulan yol' sınıfı — kapanışı bilmeyen ilk yeni çağıran yarım işlem üretirdi); tam dikey kilit (bağlı klip hiç şerit değiştiremesin — video'yu katmanlar arasında taşımayı meşru sebeple engeller, kullanıcıyı unlink'e zorlardı); tip-başına bağımsız delta (her klip kendi tipinden en yakın şeride — belirsiz UX, öngörülemez yerleşim) | Yok (davranış sözleşmesi; grup opları Dilim 4'te aynı kapanışı kullanır) |
| Otomatik AV ayrımı (ozellik-3): TEK mutate'te ÇİFT klip, yalnız `hasAudio===true` şartıyla; ses dolu şeritte YENİ track'e (notice ile); plan `planAddClipFromAsset` TEK karar noktası; kısmi başarı yasak (tavan aşımı = tümü RED) | 2026-09-01 | Video+ses ayrı şeritlere linkli düşer (CapCut ana jesti) ama undo/seçim sözleşmesi tek jest kalır — iki ayrı mutate iki undo üretirdi; sesin ikize taşınması detachAudio'nun bıraktığı ŞEKLİN AYNISI (video yarısı `audio:null`), yani export/preview yolunda yeni durum sınıfı doğmaz; ghost/drop/commit AYNI planı okuduğu için "hayalet geçerli dedi, bırakma reddetti" sınıfı yapısal olarak kapalı (pasteBlockReason emsali); dolu şeritte yeni track açmak detach'ın "varsa yeni açma" katılığından BİLİNÇLİ sapmadır — ürünün BİRİNCİL ekleme jestini dolu şerit yüzünden reddetmek düşmanca olurdu, sapma notice ile görünür; tavan kontrolü toplam yeni-track sayısını sayar (sunucu MaxTracks=50 aynası — 49 track + 2 yeni = 51 köşesi kapalı, aksi halde autosave tipli 422 ile ölürdü) | Gömülü ekle + oto-detach (iki mutate: ara durum + iki undo girdisi — "tek jest tek geri alma" bozulur); bilinmeyen `hasAudio`'da da ikiz üretmek (sessiz kaynakta ölçülmüş 422 `asset-clip-type` tuzağı — bilinmeyen sessizlik değildir); dolu şeritte TAM RET (detach kuralının yanlış genellemesi — detach ikincil bir düzenleme jestidir, ekleme ürünün ana kapısıdır) | Yok (davranış sözleşmesi; kullanıcı tek-klip eklemeyi tercih olarak isterse ayrı ayar kararı) |
| Klip grupları (ozellik-4): grupla = LINK-kapanışlı kümeye TAZE tek groupId (BİRLEŞTİRME — eski üyelikler ezilir, tek kalan üye AYNI mutate'te temizlenir); dağıt = dokunulan grupları TÜMDEN çözme; Ctrl+G / Ctrl+Shift+G | 2026-09-01 | Taze-id birleştirme tek deterministik kuraldır: iki eski grubun üyeleri seçilince "hangi grup kazanır" sorusu hiç doğmaz, kural 11 temizliği aynı mutate'te koşar (tek undo, ara geçersiz durum yok — dev doc kapısı negatif kontrolde bunu bizzat yakaladı); LINK kapanışı grubu kurarken eşi otomatik aldığı için kural 10'un "eşler özdeş groupId" kolu yapısal olarak sağlanır (kontrol değil inşa); tümden dağıtma tek-üyeli ara durumu imkânsız kılar; kısayol çifti mevcut ctrl haritasıyla çakışmaz (z y a c x v d) ve menüyle AYNI seçim-tabanlı op'lara iner | Üye çıkarma tarzı ungroup (küçülen grup ne demek: kural 11 ihlali ya da örtük temizleme belirsizliği — tek üyeli ara durum doğurur); mevcut gruba KATMA semantiği (iki grup kesişiminde belirsiz sahiplik; taze id bunu tanımsız bırakmaz); grup-içi-grup / hiyerarşi (bu turda KAPSAM DIŞI — şema düz tek alan, iç içe grup ayrı özellik kararı) | Yok (davranış sözleşmesi; iç içe grup istenirse ayrı şema+op turu) |
| J geri tarama = MOTOR DEĞİL dispatcher-seviyesi SESSİZ shuttle (ozellik-5): `shortcuts/shuttle.ts` 33 ms metronomu playhead'i STORE üzerinden geri akıtır (iç float akümülatör + kare-ızgara-yapışık yazım; kendi userSeekSeq'ini ayırt eder — dış seek/isPlaying oto-iptal; BOF'ta tam 0 + dur); kademe J tekrarıyla 1→2→4→8; K/L/Space durdurur (Space shuttle'dayken = DUR, oynatma değil — NLE uzlaşımı); J VE L'ye e.repeat muhafızı (L'de küçük savunulabilir davranış değişikliği: basılı tutmak OS auto-repeat'iyle 8x'e fırlamaz); `forwardRate` modül-let'i `useTransportStore`'a taşındı; PlayerPanel rozeti `transport-shuttle-note` ('Geri tarama {n}x — ses kapalı' / 'İleri {n}x') | 2026-09-01 | KULLANICI KARARI (2026-09-01, AskUserQuestion): J = sessiz kare-adımlamalı geri tarama. `<video>` negatif playbackRate OYNATAMAZ; motor paused'ken mevcut scrub yolu (applyScrub 80 ms throttle, son-istek-kazanır) kareyi zaten getirir → sessizlik YAPISAL (hiçbir element play() almaz, ses zarfı hiç kurulmaz, ses koduna sıfır dokunuş); iç akümülatör hız dürüstlüğünü korur (store'un kare-yapışık değerinden geri hesap 30fps'te yuvarlanma-stall'ı üretir — birim testle pinli) | Motor API'sine yön/negatif-rate eklemek (her rAF'ta sert re-seek fırtınası + saat/syncElements/ses-zarfı ÜÇ kritik bölgeye dokunurdu); `SCRUB_THROTTLE_MS`'i düşürmek (fare scrub'ıyla ORTAK sabit — düşürmek decoder'ı boğar); `previewRate$` sarı rozetini yeniden kullanmak (farklı dürüstlük mesajı: o 'istenen hız elemente sığmadı' der, buradaki 'oynatma yok, sessiz tarama var') | v2 WebCodecs gerçek reverse getirince J motora bağlanır (shuttle + rozet sökülür) |
| deleteTrack kilitli-eş reddi: silinecek kliplerin link eşi BAŞKA ve KİLİTLİ bir track'teyse track silme TÜMDEN reddedilir (`trackDeleteBlockReason` → 'linked clip is on a locked track'; menü gated sözleşmesiyle otomatik gri) — özellik turu kapanış denetimi bulgusu | 2026-09-01 | deleteClips aynı durumda zaten tümden reddediyordu (ölçüldü); deleteTrack ise kilitli eşin linkId'sini SESSİZCE siliyordu — kilitli içeriğe sessiz yazım, "sessiz düzeltme yerine görünür hata" kırmızı çizgisinin ihlali sınıfı. Kilitsiz-eş davranışı (bağ temizliği aynı mutate'te) AYNEN korunur | Sessiz bağ silmeyi sürdürmek (kırmızı çizgi ihlali; deleteClips ile asimetri) | Yok (davranış sözleşmesi) |
| Kes (Ctrl+X) çifti panoya BİRLİKTE alır: cutClips kopya adımını da LINK-kapanışlı küme üzerinden yapar (pano 2 klip, yapıştırmada remint taze ortak linkId); Ctrl+C BİLİNÇLİ asimetrik — yalnız seçileni kopyalar, yarım kopya bağsız yapışır | 2026-09-01 | Kes = sil + pano (kullanıcının "silme birlikte" kararının sadık uzantısı; CapCut davranışı): eski cutClips yalnız seçileni panoya alıp çifti siliyordu → yapıştırma bağsız tek klip, ses içeriği kes-yapıştır akışında KAYBOLUYORDU (denetimde ölçüldü). Kopya belgeden bir şey eksiltmediği için kapanış istemez — remint tekil-görülen bağı zaten siler | Kesin yarım-çift bırakması (ses içeriği sessizce düşüyordu); kopyanın da kapanışlı olması (kullanıcı yalnız seçtiğini çoğaltmak isteyebilir) | Yok (davranış sözleşmesi) |
| Scrub çukuru = SAHİPLİK-BAĞLI SON İYİ KARE (ozellik-fix, J yabancı kare): videoDrawItem readyState çukurunda slotun son yüklenen karesini çizmeye devam eder ama YALNIZ `clipId+epoch` sahipliği tutuyorsa (el değiştiren slot HİÇBİR ŞEY çizmez); paused upload damgası `'seeked'` olayıyla düşürülür (currentTime'ın set-anı değeri damga OLAMAZ — seek başlar başlamaz hedefi okur); scheduler ileri preload'un AYNASI `PRELOAD_LOOKBEHIND_US` (1 sn) ile az önce biten klibi kendi SONUNDA ılık tutar + applyScrub/preciseSeek aktifleşen ısınmış elementten seek-ÖNCESİ taban yakalar (kaynak-penceresi bekçili) | 2026-09-02 | Kullanıcı bildirimi ölçümle doğrulandı: her scrub seek'i readyState'i 62-86 ms HAVE_CURRENT_DATA altına düşürür (Chromium, gerçek proxy; ileri/geri fark etmez) ve eski "çizme" yolu katmanı ARKA PLANA düşürüyordu — sürekli geri taramada ~160 ms'de bir siyah flaş (örneklerin %55-61'i), sınırda geriye-preload'suzluk +~240 ms soğuk pencere; düzeltme sonrası aynı probe 6 rejimde 6/6 SIFIR ihlal, ileri-oynatma kontrolü yeşil kaldı. Sahiplik şartı yabancı-kare bekçisidir: kısa arka plan, başka klibin karesinden İYİDİR. Dürüst kalan maliyet: 8x taramada rAF kadansı ~25 fps (taze karenin gerçekten yüklenmesi; içerik yeniliği zaten 80 ms throttle'la 12,5 Hz — poc §2.8) | Sahipliksiz son-kare çizimi (sınırda bayat dokuyu BAŞKA klibin karesi yapar — kusurun kötü hali); `renderFrame`'de "items boşsa çizimi atla" (preserveDrawingBuffer:false'ta framebuffer korunmaz + katman-başına değil kare-başına örtü olur, çok katmanlıda yanlış); readyState düzelene dek render'ı geciktirmek (paused rAF döngüsü sözleşmesi bozulur, probe/edit akışı asenkronlaşır); şüpheye düşülen `applyScrub`/settle yarışı yönünde değişiklik (H2 ÇÜRÜDÜ — ölçümde tek sırasız örnek yok, seekSeq sözleşmesi aynen) | v2 WebCodecs motoru scrub'ı kare-kesin çizerse örtü mekanizması onunla birlikte sadeleşir |
| Zaman kodu AYRIŞTIRICISI app-yerel (`apps/editor/.../player/timecodeInput.ts`) ve hedef µs = `formatTimecode`'u yazılan metne eşitleyen EN KÜÇÜK tamsayı = `ceil(kare·den·1e6/num)`, taşma-güvenli tamsayı aritmetiğiyle (panel-1a) | 2026-09-03 | Paylaşılan `time.ts` TS↔C# BİREBİR sözleşmesidir; C# zaman kodu ÜRETİR ama ayrıştırmaz — ayrıştırıcıyı oraya koymak ya ölü ikiz doğururdu ya da "her iki dil de uygular" kuralını sessizce delerdi. Tavan seçimi ölçülmüş bir off-by-one'ı kapatır: `formatTimecode` kareyi FLOOR ile sayar, `frameToUs` half-up yuvarlar → 30/1'de kare 1 = 33 333 µs ve `formatTimecode(33333)` "00:00:00:00" der (çapraz-dil vektörü `time-vectors.json` bunu çiviliyor), yani alan kullanıcının yazdığı `:01`'i tazelenince KAYBEDERDİ. Ayrışma 30/1 ve 24000/1001'de doğar, 25/1 · 30000/1001 · 60/1'de doğmaz — üçü de teste çivili; round-trip taraması paketten GERÇEK `formatTimecode`+`usToFrame` import ederek ters yönü ileri yöne bağlar | `time.ts`'e koymak (ölü C# ikizi ya da simetri ihlali); `frameToUs`'u yeniden kullanmak (ölçülen off-by-one — e2e negatif kontrolü `Expected 33334 / Received 33333` ile kırmızı); tek parça `ceil(f*den*1e6/num)` (uzun zaman kodlarında 2^53 taşması); `formatTimecode`'u half-up'a çevirmek (C# + vektör + golden'lar kırılırdı — kod doğruydu, DOKÜMAN yanlıştı: `rendering-semantics §1.5` düzeltildi) | C# bir gün zaman kodu ayrıştırırsa ortak sözleşmeye taşınır (o zaman vektör dosyası ters yönü de taşımalı) |
| Zaman kodu okuması SAAT'tir (`1:30:00` = 1 sa 30 dk; baştaki alan takvim-serbest, iç alanlar katı, sonuç 24 saatle sınırlı) ve proje sonu kelepçesinin ÜST SINIRI tek tanımdır (`timelineOps.projectEndUs`): DOM yolları (ok/step tuşları, cetvel scrub'ı, cetvel menüsü) ortak `clampPlayheadUs`'tan geçer, saf kalması gereken zaman kodu alanı ise AYNI SINIR DEĞERİNİ kendi içinde uygular — iki yolun aynı belgede aynı sayıda durduğu birim testiyle çivili (panel-1b) | 2026-09-03 | KULLANICI KARARLARI (AskUserQuestion, 2026-09-02): "esnek/saat okuması" ve "hepsi kelepçelensin". Saat okuması NLE uzlaşımıdır ve `HH:MM:SS:FF` gösterimiyle aynı yönde okunur; kısa yazımlar (`90`, `1:30`) baştaki alanın takvim-serbestliğinden doğar. Kelepçenin TEK tanımı zorunlu: alan bir yerde, ok tuşu başka yerde dursaydı "proje sonu" iki farklı sayı olurdu — üst sınır `timelineOps.projectEndUs`'tur (End tuşu, fit, autoFit, cetvel zaten onu okuyor). PAYLAŞILAN ŞEY FONKSİYON DEĞİL SINIR DEĞERİDİR: `timecodeInput.ts` DOM'suz/fırlatmayan saf modül olarak kalsın diye kelepçeyi kendi içinde uygular; ikizliği `timelineOps.test.ts` çiviliyor ("yazılan zaman kodu ile ok tuşu AYNI sonda durur"). Cetvel bağlam menüsünün "işaret ekle" hedefi de aynı kelepçeye bağlandı (denetim bulgusu: menü hedefi proje sonunun ötesinde işaret bırakabiliyordu); `player/core/resolve.projectDurationUs` motorun KENDİ model süresi olarak yerinde kalır. Kelepçe SESSİZ değildir: alan 'Proje sonuna oturtuldu' der, boş projede ayrı kod ('Proje boş — playhead başta kaldı') | Dakika:saniye:kare okuması (`1:30:00` = 1 dk 30 sn 0 kare — gösterimle ters yönde okunur, kullanıcı kararına aykırı); kelepçeyi yalnız alana koymak (ok tuşu/cetvel içeriğin ötesine kaçardı — üç ayrı doğruluk); kelepçeyi `setPlayheadUs` store yazarına koymak (motor saati ve shuttle da oradan geçer; playback sonunda motorun kendi saatini kırpmak yeni bir yarış sınıfı açardı); 24 saat sınırını kaldırmak (girdi tamsayı aritmetiğini sınırsız büyütür; sınır GÖRÜNÜR tipli ret verir, sessiz taşma değil) | Kullanıcı dakika okuması isterse dilbilgisi tablosu tek yerden değişir; boş-proje davranışı (üst sınır 0) ürün kararıyla gevşetilebilir |
| `scrollY` kelepçesi jestlerden ÇIKARILDI: sınır tanımı `pan.maxScrollY`/`clampScrollY` (saf), yazım tek yol `TimelinePanel.applyScrollY`, ayrıca `[viewport.h, tracks.length]` değişiminde YENİDEN kelepçe (panel-2a) | 2026-09-03 | Kelepçe iki jest gövdesine KOPYALANMIŞTI (wheel + orta-tuş pan), yani yalnız kullanıcı kaydırırken uygulanabiliyordu. Sınırın KENDİSİ değişince (undo ile track silme, panelin büyümesi) bayat `scrollY` sınır dışında kalıyor ve İKİ şey birden bozuluyordu: altta boş şerit (başlık kolonu + canvas yukarıda takılı) ve `contentY = y - RULER_H + scrollY` bayat okunduğu için BİR SATIR kaymış hit-test (kullanıcı kliplere tıklıyor, seçim olmuyor). Kusur bugün Ctrl+Z ile üretilebiliyordu; dikey boyutlandırma onu HER büyütmede tetikleyecekti — bu yüzden AYRI ve ÖNCE gelen commit. Kanıt gerçek girdiyle: `e2e/timeline-scroll-clamp.spec.ts` commit öncesi `Expected 684.5 / Received 545.5` (139 px bayat) ve seçim `[]` ile kırmızıydı | Kelepçeyi jestlerde bırakıp yalnız boyutlandırma yolunda ikinci bir düzeltme çağırmak (üçüncü kopya + aynı sınıf hatanın yeni yolu); `panScrollY`'yi kelepçesiz yapıp tek yere indirmek (kendi birim testleri saf delta matematiğini çiviliyor — çift kelepçe zaten idempotent); `scrollY`'yi editorStore'a taşımak (panel onu bilinçli yerel tutuyor: KeyframeStripOverlay prop olarak alıyor, store aboneliği oynatma sırasında yeniden render riskini büyütürdü — kusurun nedeni sahiplik değil KOPYA formüldü) | Dikey kaydırma bir gün store'a taşınırsa kelepçe yazma yolu store setter'ına iner (saf fonksiyonlar yerinde kalır) |
| Timeline yüksekliği AYRI feature-yerel store + localStorage'da (`videoedit.timelineHeight.v1`); ÜÇ ALAN: kalıcı `preferredPx` (KULLANICI NİYETİ) ile efemeral `headerPx`/`availablePx` AYRI, efektif değer saf `clampTimelineHeight`'ten (panel-2b) | 2026-09-03 | KULLANICI KARARI (AskUserQuestion, 2026-09-02): yükseklik "tarayıcıda kalıcı, belgeye YAZILMAZ", max "oynatıcıya ≥160 px bırakır". Niyet/efektif ayrımı zorunlu: pencere küçülünce efektif yükseklik kelepçelenmeli ama tercih SİLİNMEMELİ — küçük bir pencerede geçen tek oturum kullanıcının kalıcı tercihini yok etmemeli (pencere büyüyünce geri gelir; birim testi bunu çiviliyor). Kelepçenin TEK sahibi saf fonksiyondur: min = ölçülen başlık + `RULER_H+TRACK_H+TRACK_GAP+NEW_TRACK_ZONE_H` (bir tam satır + "buraya bırak" bölgesi hep erişilebilir), max = alan − `MIN_PLAYER_H`, sonuç ayrıca alanı aşamaz; depolama boş/bozukken `DEFAULT_TIMELINE_H = 280` — yani özellik öncesi görünümle BİREBİR aynı (mevcut e2e zemini korunur). Yazma yalnız COMMIT anında (sürükleme sırasında `persist:false`): her karede senkron I/O jesti yavaşlatırdı | Yüksekliği timeline dokümanına yazmak (kullanıcı kararına aykırı; autosave/undo/çakışma yüzeyine görünüm tercihi sokardı, aynı projeyi başka ekranda açan kullanıcıya yabancı düzen verirdi); tek alan tutup yalnız efektif değeri saklamak (dar pencerede tercih KALICI olarak ölürdü); `editorStore`'a koymak (orası bilinçli olarak KALICI OLMAYAN görünüm durumu — kalıcılık ve ölçüm alanları ömür karıştırırdı); Tailwind sınıfıyla yükseklik (JIT çalışma zamanı değeri üretemez; sabit kademeli sınıf seti sürüklemeyi ızgaraya hapsederdi) | Yükseklik bir gün hesap/profil düzeyinde senkronlanmak istenirse anahtar sunucu tarafına taşınır (saf kelepçe ve niyet/efektif ayrımı aynen kalır) |
| Ses ölçeri = master'a PARALEL YAPRAK AnalyserNode tap'i (explicit stereo → splitter → 2 analyser) | 2026-09-03 | §8.3'ün "önizleme zincirinin SONUNA node konmaz" kuralı korunur: Web Audio'da fan-out `destination`'a gideni değiştirmez. Bu YERLEŞİM `audioGraphTopology.test.ts` ile makine tarafından çivilenir; YÜK TAŞIYAN iddia DOSYA DÜZEYİNDEDİR: yorumlar silinmiş kaynakta `destination` kelimesi TEK satırda geçebilir ve o satır master'ın kendi bağlantısıdır (yanında daha dar gövde taramaları: master→destination tap'ten ÖNCE, tap gövdesi `destination`'a dokunmaz, analyser çıkışları bağlanmaz). Yalnız gövdeyi tarayan ilk sürüm KÖRDÜ — tap `ensureContext`'ten `destination`'a bağlanınca dört iddia da yeşil kalıyordu (denetimde ölçüldü); dosya düzeyindeki iddia yerel takma adı da yakalar. Kapsam: BU DOSYA — `meterTap` private olduğu için dışarıdan erişilemez. DÜRÜSTLÜK: `audio-parity.spec.ts` bunun kanıtı DEĞİLDİR — o spec AudioGraph'ı bilinçli KULLANMAZ (offline context'te `createMediaElementSource` yok), topolojiyi sayfada yeniden kurar; denetimde ÖLÇÜLDÜ: gerçek `master.gain`'i 0'a çekmek (tam sessizlik) o spec'i bit-birebir yeşil bırakıyor. Tarayıcı çıkışını yakalayan gerçek davranış testi bu koşum düzeneğinde mümkün değil, o yüzden iddia yapısal muhafızla sınırlı tutuldu. Tap'in explicit stereo olması zorunlu: `master` kanal sayısını girdilerinden alır ('max'), tek mono klipte splitter'ın sağ çıkışı sessiz kalırdı — upmix §8.5 ile birebir | Seri node / DynamicsCompressor (§8.3 ihlali); `destination`'ı taplamak (imkânsız — çıkışı yok); TEK analyser (spec gereği mono'ya downmix eder, stereo iddiası ölürdü) | Yaprak analyser'ın işlenmediği bir tarayıcı çıkarsa `analyser → Gain(0) → destination` yedeğine geçilir (duyulur çıkışa +0 katar) |
| Ölçüm `getFloatTimeDomainData`, fftSize 2048, örnekleme aralığı `METER_INTERVAL_MS = 33` — bu bir TABANDIR, sabit hız değil | 2026-09-03 | Byte veri ±1'e KIRPAR: limitersiz önizlemede 1.0 üstü tepe görünmez olur ve klip uyarısı yapısal olarak yalan söylerdi (§8.3 asimetrisi tam da orada yaşıyor). 2048 pencere 48 kHz'de 42,7 ms ≥ 33 ms adım → pencereler ÖRTÜŞÜR, iki okuma arasına örnek düşmez. Örnekleme motorun rAF'ine bindiği için gerçek kadans aralığın bir KAREYE YUKARI YUVARLANMIŞ hâlidir: 60 Hz ekranda 33,3 ms (30 Hz), 165 Hz ekranda 36,4 ms (27,5 Hz) — ikisi de ÖLÇÜLDÜ; "30 Hz" diye yazmak ekrana bağlı bir sayıyı sabit gibi göstermek olurdu (denetim bulgusu) | 1024 pencere (33 ms'te delik bırakır); 60 Hz kadans (aynı pencereyi ~2,5 kez okur); AudioWorklet (jest yolunda async modül yükü, POC için ağır) | Kadans 12 Hz'e indirilirse fftSize 4096'ya çıkar |
| Ölçer matematiği + TÜM metinler saf `core/meter.ts`'te; balistik DUVAR SAATİ sürer | 2026-09-03 | vitest ortamı `node` ve `.tsx` testi almıyor → bileşende bırakılan her sayı/cümle test edilemez olurdu (scheduler.previewShortfallNote emsali). Duvar saati: rAF kısıldığında (arka plan sekmesi, headless ~12 Hz) kare-tabanlı düşüş oranı hakkında YALAN söylerdi; birim testi 30 Hz ve 12 Hz adımların AYNI hold'a indiğini ölçüyor | Balistiği motora koymak (görüntü politikası fizik katmanına sızardı); kare-sayısı tabanlı decay | Yok |
| Ölçer `readMeter()` ctx yok/çalışmıyorken NULL döner; UI "0" değil GEREKÇE gösterir | 2026-09-03 | Sıfır seviye "miks sessiz" demektir; gerçek durum "ölçülecek miks yok"tur (AudioContext ilk oynatmaya kadar kurulmaz — tarayıcı kuralı). Donmuş bir ölçer yalan söyleyen ölçerdir. e2e ilk iddiası tam bunu çiviler: jestten önce `reason=no-context` + "Ölçüm yok" | Sıfır döndürmek; suspended context'te analyser tamponundaki BAYAT veriyi canlı gibi göstermek | Yok |
| Ölçer TimelinePanel gövde satırının 3. hücresi (64 px), canvas wrap'ın KARDEŞİ | 2026-09-03 | e2e harness wrap'ı "doğrudan çocuklarında ≥3 canvas olan ilk div" sezgisiyle bulur ve `canvas[1]`i gövde imzası için hash'ler; wrap'a canvas eklemek İKİ test sözleşmesini birden yeniden yazardı. Kardeş hücre wrap'ı 3'te tutar ve ölçer track satırlarıyla hizalı kalır | App.tsx'e 4. grid sütunu (3 satır layout değişikliği + inspector'dan kopma); wrap'ın içine canvas; transport barı (kullanıcının istediği yer değil) | Timeline genişliği kritikleşirse görünürlük anahtarı eklenir (backlog) |
